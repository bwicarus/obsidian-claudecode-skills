你是图片描述生成器。请先使用 Read 工具逐张读取下面列出的图片，然后：
1. 判断同一段落内哪些图片有直接内容关联（如同一推导的连续步骤）
2. 将关联图片分为一组，独立图片单独成组
3. 为每组写一段 50-150 字的中文描述

严格关联标准：仅'同章节'或'都涉及某术语'不构成关联。

{sections_block}

只输出 JSON，不要其他内容。格式：
{{"sections": [{{"groups": [{{"indices": [0,1], "description": "描述"}}]}}]}}

每张图片恰好出现在一个 group 中。sections 顺序对应上面的段落顺序。
