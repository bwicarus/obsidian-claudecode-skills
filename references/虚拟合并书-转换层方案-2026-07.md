# 虚拟合并书:问题复盘与转换层方案(2026-07-19,供外部审查)

> ⚠ **2026-07-19 供外部审查的方案稿，v2 已落地，勿把它当待办**：文内 §三（v1 两咽喉方案）已被同文件的「v2 修订」节明确凌驾，而 v2 也已实现 —— 稳定 `group_id` + manifest/revision + 双向 resolver + fail-closed 见 `scripts/lib/vbook.py`（`94e41401`，文件头即引用本文 v2 节为规格），路由强制策略分类见 `_server_deploy/vbook_route_policy.py` + `tests/test_vbook_route_policy.py`（另有 `test_vbook_resolver.py` / `test_vbook_gate.py`），合并书强制图片模式见 `_server_deploy/static/pdf/reader.src/03-loader.js:137`，「单本书＝只有一个成员的合并书」的统一见 `abb63e0f`。§四「请审查者回答的问题」是当时的征询，不是未决事项。另：方案通篇假设阅读器前端由 Pi 以 PWA 交付，这一前提已不成立 —— PWA 阅读器页面 2026-08-14 起返 410，前端改由 App 的 ReaderBundle 与扩展 `vendor/` 副本投递（见 CLAUDE.md「产品边界」与 `references/deployment-workflow.md`）。

> 功能定义(用户设计):同名+partN 的分卷文件在系统里视作一本书——书架一条、页码连续(part1 有 x 页
> 则 part2 第 1 页显示为 x+1)、滚动跨卷零等待、AI/概念网/技能树把它当一本书;原文件不动,纯虚拟层。
> 命名助手:重命名输入与已存在书同名 → 自动建议 partN+1(可改)。

---

## 一、已交付且状态良好的部分(不在本次方案范围,保留)

| 件 | 状态 |
|---|---|
| 核心分组模块 `scripts/lib/book_groups.py`(PART_RE/成员/页数缓存 fitz 权威/offset/canonical) | ✅ 实测料理師 55+80=135 |
| 书架合并显示(📚 合卷×N·总页;代表卷=最近打开;其余卷折叠) | ✅ 上线 |
| 重命名 partN+1 自动建议 | ✅ 上线 |
| 概念网/注册表按组归一(canonical=最小卷;跨卷向前搜索,全局页码;锚点转回真文件+局部页) | ✅ 上线 |
| 深链仲裁修复(显式 ?page= 的 page_ts=now 必胜本地续读——顺带修了收藏/技能树深链被旧位置拽走的老bug) | ✅ 上线 |

## 二、失败复盘:bolt-on 特例路线(已废弃并闸掉)

阅读器侧我走了"在旧架构旁边挂特例"的路线,四轮补丁每轮修一个问题又冒出新的:

| 轮 | 做法 | 暴露的问题 |
|---|---|---|
| 1 | 显示偏移叠加(_dispPage+组偏移)+ 边界浮标按钮 | 「加载中」占位页码与顶栏不一致(3处没走偏移);浮标在连续滚动模式不出现(该模式绕过挂钩子的函数直改 DOM) |
| 2 | 滚动越界自动跳卷(wheel/touch 累积阈值) | touchmove 里每次读 scrollHeight 强制重排 → 严重卡顿;跳卷落地被本地旧续读仲裁拽回(深链 ts=0 必输) |
| 3 | 回流修复(rAF 边缘标志)+ 深链必胜 + 武装/驻留防乒乓 | 翻回上一卷仍卡(落地即在底部边缘,回弹又触发=乒乓) |
| 4 | 盒子模型 bolt-on(外卷页盒子接在末尾+停稳换会话) | **单页/双页等模式大量异常**——这些模式的内部逻辑(页判定/滚动条/跳页钳制/spread 行/插入页/内存卸载)都不知道"外卷盒子"的存在 |

**根因(不是哪个补丁写错,是路线错)**:每一层功能都被要求"知道分卷的存在",而阅读器有 100+ 处
页码/文件语义(下节实测 114 个 API 端点、31 个前端模块)——特例打不完。这违反了本项目自己的
统一控制层铁律:"让中间层去适应旧代码,不为新形态另造上层建筑"。

**当前止血状态**:`__GRP` 默认 null(全部组行为 no-op),阅读器行为=合并功能之前;`&vmerge=1` 保留实验入口。
第一节那些不经过阅读器内部的件全部保留。

## 三、转换层方案(提议,待审查)

### 原则

**程序其余部分零改动。分卷只在两个咽喉处被翻译掉,其余代码(全部 31 个前端模块、全部功能)
永远只看见"一本 135 页的书"。**

### 咽喉 1:文档门面(客户端唯一改动点)

阅读器的世界只依赖三样:`pdfDoc`(numPages/getPage(n))、`FILE_REL`、发往服务端的 `(file,page)`。
- 合并态打开:`?file=<canonical>&merged=1`;
- `pdfDoc` = **合并门面对象**:`numPages=135`;`getPage(60)` → 懒加载 part2 的真 PDF.js 文档 → 返回其第 5 页。
  PDF.js 页对象自带渲染上下文,单页/双页/连续/缩放/滚动/文本层**全部照常**——它们只跟 pdfDoc 说话。
- **先例**:图片模式已经用同样的 pdfDoc shim 让全部代码无感知运行(03-loader:"其余代码靠 shim 的
  getPage().getViewport() 照常工作")。本方案是同一模式的第二个实例,不是新发明。
- `FILE_REL` = canonical;客户端内部页码**直接就是全局页**(v1 的全部显示偏移 hack 删除,不再有两套页码)。
- 各成员的 PDF 字节按需经现有 pdf_url 通道加载(range 支持已有);滚动到外卷区时才拉那卷 → 天然满足
  "滚动零等待+渐进加载"(占位盒子机制阅读器本来就有)。

### 咽喉 2:服务端 (file,page) 翻译(存储不变式的唯一保证点)

客户端继续盲传 `FILE_REL+page`(零改动)。服务端在 `/pdf/api/*` 入口统一翻译:
- 识别:`file=<canonical>` 且请求带 `merged=1`(客户端 fetch 不改——由模板给 `__PDF_CFG.file_rel`
  直接放入带标记的虚拟 rel,如 `canonical` + cfg.merged 标志,由**一个**既有的 URL 构造点追加;
  具体注入点实施时定,原则是客户端仍然只有一处知道)。
- 翻译:`(canonical, 全局60)` → `(料理师part2.pdf, 局部5)` → 交原 handler。
- **不变式**:所有 sidecar/缓存/埋点永远存**真文件+局部页**(由咽喉保证,不再依赖每个功能自觉);
  合并只是视图。已有的每卷高亮/便签/墨迹零迁移、在合并视图自动正确出现。

### 端点翻译清单(实测枚举,114 个)

- **45 个逐页端点(file+page)= 纯透传翻译**:page-image/page-chars/page-text/page-translate/
  highlights(单页)/ink/dict 系/translate/figures/userpages/insert-page/dwell 埋点等。
  翻译进(含 POST body 里的 page),响应原样(它们只关于翻译后的那一页)。
- **44 个整本级端点(只 file)分三类**:
  a. **列表扇入**(合并视图要看到全组):GET highlights(整本)/notes 列表/book-figures/目录 toc/
     书内搜索 /api/search、vocab-mastery-map 等 → 对每个成员调原逻辑,页码+offset 合并返回;
  b. **真·按卷操作**(不该合并):压缩/预处理/预热/删除/重命名/上传 → 作用于**真实成员文件**,
     合并态下 UI 语义=作用于当前所在卷(或明确列出成员);
  c. **EPUB 专属/与页无关**:不受影响。
- 剩余 25 个不带 file 的端点不受影响。

### 分阶段实施与验收

| 阶段 | 内容 | 验收 |
|---|---|---|
| 0 | 端点清单逐个核对分类(上表是机器初分,人工过一遍 45+44) | 清单签字 |
| 1 | 服务端翻译模块(纯函数+单元测试:全局↔局部/越界/非组直通)+ 咽喉挂载 | 测试全绿;非合并请求零行为变化(回归) |
| 2 | 客户端 pdfDoc 门面 + merged 打开链路;先只验读路径:全模式滚动/跳页/选词/查词/翻译跨卷 | 手测清单:单页/双页/连续×跨卷边界 |
| 3 | 列表扇入端点(a 类逐个) + 写路径全量(高亮/便签/墨迹跨卷落真文件) | 每端点:合并视图操作→检查落盘在正确成员 sidecar |
| 4 | 位置(canonical+全局页,与既有 per-part 记录仲裁)/书架入口切 merged/清理 v1 残留 hack | 两晚真实使用 |

### 风险与开放问题(请审查者重点看)

1. **虚拟 rel 的泄漏面**:merged 态的 canonical/全局页会不会流进翻译层覆盖不到的通道——
   SSE 总线 ctx、assistant/语音的 file 参数、MCP 工具、注意力埋点(埋点必须记真 rel,否则概念网
   /画像数据被污染)。计划:翻译层挂在 bp 入口,上述通道全走 /pdf/api → 应被覆盖,但需逐通道核实。
2. **列表扇入清单完整性**:44 个整本级端点的 a/b 分类是机器初分,漏一个=合并视图缺一块数据。
3. **PDF.js 多文档内存**:两个(将来更多)成员文档同时打开;计划 LRU 只保近 2 个成员 doc,远卷销毁。
4. **spread(双页)跨卷行**:全局第 55/56 页会被排进同一行(分属两文件)——门面 getPage 天然支持,
   但需确认 spread 行构造只用 pdfDoc(初查是)。
5. **页码对齐(印刷页)语义**:per-卷特性;合并态下对齐输入应作用于"当前所在卷",UI 需说明。
6. **性能**:每请求一次组查表(内存缓存 O(1)),预期可忽略;需在阶段 1 实测。
7. **EPUB 分卷**:本方案只覆盖 PDF;EPUB 无页概念,暂只做书架折叠。

## 四、请审查者回答的问题

A. 两咽喉的切法是否有遗漏的第三通道?(尤其埋点/助手上下文)
B. "客户端内部页码=全局页"是否比"局部页+显示偏移"更险?(我认为更稳:单一页码语义,v1 的两套页码正是问题源)
C. 阶段划分是否该把"写路径"提前(阶段3→2)以尽早暴露 sidecar 落盘错误?
D. 有没有比 pdfDoc 门面更小的客户端切口?

---

# v2 修订(2026-07-19,采纳外部审查——本节凌驾上文 §三/§推荐顺序)

审查五条核心主张逐条对真实代码核实**全部成立**,方案按其修正:

## 修正一:废弃「canonical+merged 标志」→ 稳定虚拟身份 + fail-closed

- 不再用 `file=part1.pdf&page=global&merged=1`(任何一条通道漏了 merged 标志 = **静默把第 60 页
  写进 part1**,污染而非报错)。
- 改用:`view_ref = vbook:<稳定 group_id>` + `global_page` + `revision`。
- **未适配的旧 handler 收到 `vbook:` 前缀必须 fail-closed 报错**——宁可功能缺失,绝不静默污染。
- canonical_rel(最小卷号)不能当稳定主键(新增 part0/删 part1/重命名都会改变身份)→ 持久化独立
  **manifest**:`{group_id, revision, members:[{num,rel,pages,mtime_ns,size,offset}]}`;
  revision 随成员结构变化;请求带 revision,服务端不匹配 → `409 manifest_stale`(绝不静默重解释后写入)。
- 成员页数为 0 或卷号重复 → 拒绝合并。book_groups 升级:内存 registry+锁+`mtime_ns+size` 失效+原子写。

## 修正二:「两个咽喉」→ 双向位置模型 + 三类适配器

坐标分层(**全局页只属于视图,真实卷+局部页才是持久化真相**):

| 坐标 | 用途 | 持久真相 |
|---|---|---|
| ViewLocation(group_id, global_page, revision) | 阅读器/AI 报页/跳页/UI | 否 |
| SourceLocation(member_rel, local_page) | sidecar/注意力/任务/缓存/审计 | **是** |
| PrintedPage(member_rel, printed) | 印刷页显示 | 逐卷设置 |

**全进程共享的领域服务**(不是只挂 Flask middleware——实锤的第三通道:assistant._page_text 自己读文件、
voice relay 从 WS URL 拿 file/page、MCP→assistant 工具、task_runtime 持久化 run["file"]、SSE 发物理
文件而前端 `ev.file===FILE_REL` 严格比较、sendBeacon/EventSource/img.src 不走 fetch 包装):

```
resolve_view(view_ref, global_page, revision) -> SourceLocation
resolve_pages(view_ref, [55,56], revision)    -> [part1/p55, part2/p1]
to_view(member_rel, local_page, view_ref)     -> global_page      # 反向:响应/SSE/搜索结果/job 结果 globalize
members(view_ref) / group_revision(view_ref)
```

**必须双向**:page-text 返回局部 page+单卷 total(实锤 pdf_reader:2789)之类的响应要反译;
SSE 事件、client_action、搜索结果、异步 job 结果同理。

## 修正三:路由清单从运行时 url_map 生成 + 强制策略声明

114 只是 @bp.route 静态扫的(实锤漏了 grammar_reader/favorites_reader 的 add_url_rule 动态注册,
另有 TOC/SSE/EPUB assistant)。阶段 0 改为:运行时 `app.url_map` 全量导出,**每个路由必须声明策略**
`PAGE|BOOK_FANIN|ID_ROUTED|MEMBER_REQUIRED|JOB_OR_RANGE|GLOBAL|NONE|EPUB`,新增未分类路由 → 测试直接失败。

## 修正四:客户端第一阶段=合并书强制图片模式(shim 已现成)

classic 多文档门面(worker/LRU/数百 MB ArrayBuffer 并存,且 loader 会整本缓存)推迟到后期。
第一阶段:book-meta 返回组总页/revision/成员尺寸 → 图片模式 shim `numPages=group.total`、
`getPage(global)` 返回对应成员 viewport → 服务端把 page-image/page-chars 映射真实成员。
零多文档问题。后续 classic 门面的 LRU 需 loading 去重+active pin+idle grace+字节预算,不是"留2个杀第3个"。
「滚动零等待」加距边界 2-3 页/空闲时预开下一卷。
两个 pdfDoc 藏不住的逐卷状态:印刷页 offset、crop 去边——按**每个全局页**定位成员后应用(55/56 spread
同屏跨卷,没有唯一"当前卷")。

## 修正五:读写竖切提前 + 结构变更合并态禁用

只读页面本身就在自动写(位置/dwell 埋点/高亮/墨迹含 sendBeacon/便签/SSE 回放/assistant 读写)→
阶段 2 就做**一条完整读写竖切**,验收直接查磁盘:global60 的数据只允许落在 part2/local5。
真插页/删页/重命名/压缩=结构变更,合并首版**直接禁用**(实锤:29-optimistic-delete 直改
pdfDoc.numPages 并全局重编号,合并态必错);后续支持时:锁组→改真成员→新 manifest/revision→
失效缓存→强制阅读器重载映射。

存储格式(位置/收藏/深链/注意力):`{"source":{file,page}(真相), "view":{group_id,global_page,revision}(投影)}`。

## v2 实施顺序(替代 v1 分阶段表)

1. 稳定 group_id + manifest + revision + 双向 resolver(领域服务,单元测试:双向/越界/stale/fail-closed)
2. 运行时 url_map 强制分类清单(未分类=测试失败)
3. merged 图片模式 + 最小读写竖切(跨卷读、高亮、墨迹、便签、位置、dwell、assistant/voice、SSE)
4. BOOK_FANIN / ID_ROUTED / 搜索/TOC/列表响应反译
5. 逐卷设置(印刷页/crop)+ member operations;结构性插删仍禁用
6. classic 多文档门面 + 安全 LRU
7. 真实使用回归后书架默认开启,删 v1 残留
