# 已保存任务

先取得已保存任务的实时清单和 schema，再按名称解析；不要只凭用户记忆猜名称。

## 两种语义

- 重新生成型：以保存的原始意图为主，叠加本次参数、数据源和调整，再结合当前书页重新规划。
  它需要当前 Codex 重新在场，不能回放旧结果。
- 机械轨迹型：按已验证路线回放，不需要研究 agent，也不应迁移成 CLI 任务。

部分保存的任务只能执行保存范围内的步骤，不能补做路线之外的高亮、卡片或查书。开始任何写入
后若返回不确定，不得从头重跑。当前会话没有任务清单或运行能力时，明确返回“已保存任务服务
能力未暴露”，不要启动旧 CLI worker 兜底。
