/* AUTO-GENERATED by build.py — 源=_server_deploy/static/pdf/rc-phrasepop.js 逐字包装,勿手改 */
;(function(document, fetch){
if (window.__bwPwaProviderOnly) return;
/* rc-phrasepop.js — 统一控制层:F6「词组」浮层(共享,PDF/EPUB 通用)。
 * 忠实移植自 PDF 阅读器 reader.src/15-phrase-wordpop.js 的
 *   showPhrasePopover / _phraseFav(收藏) / _wordPopMaster 的 phrase 分支(标掌握) /
 *   _renderPitch / _isJaWord,以及 _loadPhraseFavs。端点、字段、按钮文案、声调算法 —— 全部照搬。
 *
 * 自包含:复用 #word-pop 元素 + .wp-* 视觉(与 rc-wordpop 同款,本模块也自注入一份 guard,
 *   即便从未开过单词框也能正确显示);发音复用 rc-wordpop 暴露的 window._ttsWord / _speakOnline;
 *   _renderPitch / _isJaWord 本模块内重实现(逐字搬自 PDF,不跨模块取私有函数)。
 *
 * 与 PDF 的差异(reflow 适配,底座无关的退化,均不改 reader.src):
 *   · 定位:PDF 用 charBox;这里用 opts.rect(选区 viewport 矩形)+ _position(rect),#word-pop=fixed。
 *   · 呼吸高亮 / 收藏后乐观去下划线是字符层专属 → 退化成「底座回调」:onSolid(出结果转常亮)/
 *     onFav(收藏后底座去 mark + 刷新)/ onMastered(标掌握后底座刷新)。底座(epub-html.js)用原生
 *     <mark class="ep-phrase-hl"> 实现呼吸高亮(只包不增删可见字符,G0 偏移不漂)。
 *
 * 底座耦合全部走 opts(epub-html.js 在 onPhrase 传):
 *   opts.text         词组文本
 *   opts.rect         选区矩形(viewport,getBoundingClientRect 那种),用于定位
 *   opts.file         FREL,日语词组查 dict-jp 时带(本模块当前 dict-jp 按 word 全局查,file 预留)
 *   opts.langs        本书语言数组(决定英/日分流);EPUB 传 bookLangsArr() 让模块按声明 + 字符判英/日
 *   opts.onSolid()    出结果回调(查询返回后);底座停呼吸转常亮保持
 *   opts.onFav(text, nowFav)        收藏 toggle 成功后回调(底座:收藏→去 mark + 刷新分词/下划线)
 *   opts.onMastered(text, mastered) 标掌握 toggle 成功后回调(底座:刷新生词下划线)
 *   opts.onExplain(text)            💡 解释回调(底座:走 RC.result 解释模态)
 */
(function () {
  if (!window.RC) window.RC = {};
  var RC = window.RC;
  if (RC.phrasepop) return;

  var esc = function (s) { return String(s == null ? '' : s).replace(/&/g, '&amp;').replace(/</g, '&lt;'); };

  // ── 收藏词组 + 已掌握词组 本地投影 ──
  // 真正的用户状态由 reader-runtime/vocabulary-state.js 统一持有；这两个 Set 只保留给
  // 旧底座的同步查询接口和 vocabulary-state 尚未加载时的退化路径。服务器端点只负责
  // 兼容同步，迟到的响应不得再成为界面状态的权威来源。
  var _favSet = new Set();
  var _favDisplay = new Map();
  var _markSet = new Set();
  var _repoRecords = { favorite: new Map(), mastered: new Map() };
  var _localTouched = { favorite: new Set(), mastered: new Set() };
  var _compatQueues = { favorite: new Map(), mastered: new Map() };
  var _boundVocabularyState = null;
  var _unsubscribeVocabularyState = null;
  var _norm = function (s) { return String(s || '').trim().replace(/\s+/g, ' ').toLowerCase(); };

  function _phraseSpec(text, isJa) {
    return { kind: 'phrase', language: isJa ? 'ja' : 'en', text: text };
  }
  function _guessPhraseIsJa(text) {
    return /[぀-ヿ㐀-鿿]/.test(String(text || ''));
  }
  function _vocabularyState() {
    var state = window.BWReaderRuntime && window.BWReaderRuntime.vocabularyState;
    return state && state.CONTRACT === 'vocabulary-state/1' ? state : null;
  }
  function _paintFavButton(btn, enabled) {
    if (!btn) return;
    btn.disabled = false;
    btn.textContent = enabled ? '★ 已收藏' : '☆ 收藏为词组';
    if (btn.classList && btn.classList.toggle) btn.classList.toggle('wp-anki', enabled);
  }
  function _paintMasterButton(btn, enabled) {
    if (!btn) return;
    btn.disabled = false;
    btn.textContent = enabled ? '✓ 已掌握 100' : '☆ 标记掌握';
    btn.title = enabled ? '点击取消掌握（恢复词组下划线）' : '标记掌握 100（该词组不再标生词下划线）';
    if (btn.classList && btn.classList.toggle) btn.classList.toggle('wp-anki', enabled);
  }
  function _projectRecord(record) {
    if (!record || record.kind !== 'phrase' ||
        (record.property !== 'favorite' && record.property !== 'mastered')) return;
    var key = _norm(record.key);
    if (!key) return;
    var records = _repoRecords[record.property];
    records.set(String(record.id || [record.property, record.language || 'und', key].join(':')), record);
    var enabled = false;
    records.forEach(function (item) {
      if (_norm(item && item.key) === key && item.enabled === true) enabled = true;
    });
    if (record.property === 'favorite') {
      if (enabled) {
        _favSet.add(key);
        if (!_favDisplay.has(key)) _favDisplay.set(key, record.key || key);
      } else {
        _favSet['delete'](key);
      }
    } else {
      if (enabled) _markSet.add(key); else _markSet['delete'](key);
    }

    // 同一词组可能分别拥有英/日记录。当前按钮只跟随当前语言的精确记录，
    // 不能被另一个语言中迟到的 provider 事件误改。
    if (!_state || _norm(_state.text) !== key) return;
    var current = _propertyValue(
      _state.text,
      _state.jp,
      record.property,
      record.property === 'favorite' ? _favSet.has(key) : _markSet.has(key)
    );
    if (record.property === 'favorite') {
      _paintFavButton(document.getElementById('ep-phrase-fav-btn'), current);
    } else {
      _state.mastered = current;
      _paintMasterButton(document.getElementById('ep-phrase-master-btn'), current);
    }
  }
  function _hydrateVocabularyState(state) {
    state = state || _vocabularyState();
    if (!state) return;
    var records = [];
    try { records = state.snapshot() || []; } catch (_) {}
    records.forEach(_projectRecord);
  }
  function _bindVocabularyState() {
    var state = _vocabularyState();
    if (!state) return null;
    if (_boundVocabularyState !== state) {
      if (_unsubscribeVocabularyState) {
        try { _unsubscribeVocabularyState(); } catch (_) {}
      }
      _boundVocabularyState = state;
      _unsubscribeVocabularyState = null;
      try {
        _unsubscribeVocabularyState = state.subscribe(function (event) {
          _projectRecord(event && event.record);
        });
      } catch (_) {}
      _hydrateVocabularyState(state);
      try {
        Promise.resolve(state.ready()).then(function () {
          if (_boundVocabularyState === state) _hydrateVocabularyState(state);
        }).catch(function () {});
      } catch (_) {}
    }
    return state;
  }
  function _propertyValue(text, isJa, property, fallback) {
    // 这里不能再次执行 hydrate；_projectRecord 会用本函数刷新当前按钮，
    // 若再 hydrate 会递归投影同一份快照。
    var state = _boundVocabularyState || _vocabularyState();
    if (!state) return !!fallback;
    try {
      var record = state.lookup(_phraseSpec(text, isJa), property);
      return record ? record.enabled === true : !!fallback;
    } catch (_) {
      return !!fallback;
    }
  }
  function _writeVocabularyState(text, isJa, property, enabled, source) {
    var state = _bindVocabularyState();
    if (!state) return null;
    try {
      var transaction = property === 'favorite'
        ? state.setPhraseFavorite(_phraseSpec(text, isJa), enabled, { source: source || 'phrasepop' })
        : state.setMastered(_phraseSpec(text, isJa), enabled, { source: source || 'phrasepop' });
      if (transaction && transaction.record) _projectRecord(transaction.record);
      // 持久化失败只影响后台耐久性，绝不撤销已同步生效的本地动作。
      if (transaction && transaction.durable && transaction.durable.catch)
        transaction.durable.catch(function () {});
      return transaction;
    } catch (_) {
      return null;
    }
  }
  function _rememberFallback(text, property, enabled) {
    var key = _norm(text);
    if (!key) return;
    if (property === 'favorite') {
      if (enabled) {
        _favSet.add(key);
        _favDisplay.set(key, String(text || key));
      } else {
        _favSet['delete'](key);
      }
    } else {
      if (enabled) _markSet.add(key); else _markSet['delete'](key);
    }
  }
  function _bootstrapServerValues(values, property) {
    (Array.isArray(values) ? values : []).forEach(function (text) {
      var key = _norm(text);
      if (!key || _localTouched[property].has(key)) return;
      var state = _bindVocabularyState();
      if (!state) {
        _rememberFallback(text, property, true);
        return;
      }
      var spec = _phraseSpec(text, _guessPhraseIsJa(text));
      var existing = null;
      try { existing = state.lookup(spec, property); } catch (_) {}
      if (existing) {
        _projectRecord(existing);
        return;
      }
      _writeVocabularyState(text, spec.language === 'ja', property, true, 'phrasepop-server-bootstrap');
    });
  }
  function _queueCompatibility(property, text, task) {
    var key = _norm(text);
    var queue = _compatQueues[property];
    var previous = queue.get(key) || Promise.resolve();
    var current = previous.catch(function () {}).then(task);
    queue.set(key, current);
    current.then(function () {
      if (queue.get(key) === current) queue['delete'](key);
    }, function () {
      if (queue.get(key) === current) queue['delete'](key);
    });
    return current;
  }
  function _sendCompatibility(property, text, enabled) {
    return _queueCompatibility(property, text, function () {
      var favorite = property === 'favorite';
      var body = favorite ? { text: text } : { text: text, mark: enabled ? 'mastered' : '' };
      var request;
      var url;
      var method;
      if (favorite && enabled) {
        url = '/pdf/api/phrases';
        method = 'POST';
        // @interaction phrase.favorite.add
        request = fetch('/pdf/api/phrases', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(body)
        });
      } else if (favorite) {
        url = '/pdf/api/phrases';
        method = 'DELETE';
        // @interaction phrase.favorite.remove
        request = fetch('/pdf/api/phrases', {
          method: 'DELETE',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(body)
        });
      } else {
        url = '/pdf/api/phrase-mark';
        method = 'POST';
        // @interaction phrase.mastery.set
        request = fetch('/pdf/api/phrase-mark', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(body)
        });
      }
      return request.then(function (response) {
        return response.json();
      }).then(function (result) {
        if (result && result.ok === false) throw new Error(result.error || 'compatibility sync failed');
        // 服务器返回的 phrases/mastered 是兼容快照，不得覆盖本地较新的投影。
        return result;
      }).catch(function (error) {
        if (RC.outbox && error && error.name === 'TypeError') {
          if (favorite) {
            RC.outbox.send('phrasefav', text, url, body, method);
          } else {
            RC.outbox.send('phrase', _norm(text), url, body);
          }
        }
      });
    });
  }
  function loadFavs() {
    _bindVocabularyState();
    // @interaction phrase.state.read
    fetch('/pdf/api/phrases').then(function (r) { return r.json(); }).then(function (d) {
      if (d && d.ok) _bootstrapServerValues(d.phrases, 'favorite');
    }).catch(function () {});
    // @interaction phrase.state.read
    fetch('/pdf/api/phrase-mark').then(function (r) { return r.json(); }).then(function (d) {
      if (d && d.ok) _bootstrapServerValues(d.mastered, 'mastered');
    }).catch(function () {});
  }
  function favList() {
    _bindVocabularyState();
    return Array.from(_favSet).map(function (key) { return _favDisplay.get(key) || key; });
  }   // 给底座 _vocabTokens 做最长匹配分词

  // ── CSS(.wp-* 逐字搬自 pdf_reader.html / rc-wordpop;guard:从未开过单词框也能用)──
  var _cssInjected = false;
  function injectCss() {
    if (_cssInjected) return; _cssInjected = true;
    if (document.getElementById('rc-wordpop-css') || document.getElementById('rc-phrasepop-css')) { _cssInjected = true; if (document.getElementById('rc-phrasepop-css')) return; }
    var css = document.createElement('style'); css.id = 'rc-phrasepop-css';
    css.textContent = [
      '#word-pop{position:fixed;display:none;background:#10162a;border:1px solid #3b6db5;border-radius:10px;box-shadow:0 8px 24px rgba(0,0,0,.6);z-index:200;width:min(340px,86vw);font-size:13px;color:#cfe6ff;max-height:80vh;overflow-y:auto;overflow-x:hidden}',
      '#word-pop .wp-head{display:flex;align-items:center;gap:8px;padding:11px 14px 7px;flex-wrap:wrap}',
      '#word-pop .wp-word{font-size:17px;font-weight:600;color:#fff}',
      '#word-pop .wp-phon{color:#a8cdff;font-style:italic;font-size:12px}',
      '#word-pop .wp-pitch{display:inline-flex;align-items:flex-end;gap:0;font-size:15px;color:#cfe6ff;padding-top:4px}',
      '#word-pop .wp-pitch .pm{position:relative;padding:3px 1px 0;line-height:1.1;border-top:2px solid transparent}',
      '#word-pop .wp-pitch .pm.hi{border-top:2px solid #6fd3ff;color:#dff1ff}',
      '#word-pop .wp-pitch .pm.drop::after{content:"";position:absolute;right:-1px;top:0;height:9px;border-right:2px solid #ff8a8a}',
      '#word-pop .wp-pitch .pm-type{margin-left:6px;font-size:10px;color:#5a6680;font-style:normal;align-self:center;border:1px solid #2a3450;border-radius:4px;padding:0 4px}',
      '#word-pop .wp-speak{background:transparent;border:1px solid #3b6db5;color:#a8cdff;border-radius:50%;width:26px;height:26px;cursor:pointer;font-size:12px;padding:0;flex-shrink:0}',
      '#word-pop .wp-speak:hover{background:#244470;color:#fff}',
      '#word-pop .wp-def{padding:7px 14px 11px;line-height:1.6;color:#cfe6ff;border-top:1px solid #1f2740}',
      '#word-pop .wp-actions{display:flex;gap:8px;padding:9px 14px;border-top:1px solid #1f2740;background:#0d1322;flex-wrap:wrap}',
      '#word-pop .wp-actions button{flex:1;background:#1a2540;border:1px solid #2a3550;color:#cfe6ff;border-radius:6px;padding:8px;cursor:pointer;font-size:12px}',
      '#word-pop .wp-actions button:hover{border-color:#3b6db5}',
      '#word-pop .wp-anki{background:#13351f !important;border-color:#34d399 !important;color:#7ee2b8 !important}'
    ].join('\n');
    document.head.appendChild(css);
  }

  // ── 日语判定(照搬 15-phrase-wordpop.js::_isJaWord:含假名→是;含汉字按本书声明,未声明默认日语)──
  function _isJaWord(w) {
    if (/[぀-ヿ]/.test(w)) return true;
    if (!/[㐀-鿿]/.test(w)) return false;
    var declared = (_ctx.langs || []).length > 0;
    return declared ? _ctx.langs.indexOf('ja') >= 0 : true;
  }
  // ── 日语声调(ピッチアクセント),逐字搬自 15-phrase-wordpop.js::_renderPitch ──
  function _renderPitch(reading, accent) {
    var small = 'ぁぃぅぇぉゃゅょゎァィゥェォャュョヮ';
    var mora = [];
    for (var ci = 0; ci < reading.length; ci++) {
      var ch = reading[ci];
      if (small.indexOf(ch) >= 0 && mora.length) mora[mora.length - 1] += ch;
      else mora.push(ch);
    }
    var n = mora.length;
    if (!n) return '';
    var hi = function (i) {
      if (accent === 0) return i >= 1;
      if (accent === 1) return i === 0;
      return i >= 1 && i < accent;
    };
    var html = '<span class="wp-pitch" title="声调型 ' +
      (accent === 0 ? '平板' : accent === 1 ? '頭高' : '第' + accent + '拍后降') + '">';
    for (var i = 0; i < n; i++) {
      var h = hi(i);
      var drop = (accent >= 1 && i + 1 === accent);
      html += '<span class="pm' + (h ? ' hi' : '') + (drop ? ' drop' : '') + '">' + mora[i] + '</span>';
    }
    var tlabel = accent === 0 ? '平板' : accent === 1 ? '頭高' : '[' + accent + ']';
    html += '<span class="pm-type">' + tlabel + '</span></span>';
    return html;
  }

  // ── 定位(rc-wordpop _positionPop 套路:左右钳进视口、放不下翻上、顶≥54)──
  function _position(pop, rect) {
    if (!pop) return;
    if (!rect) { pop.style.left = Math.max(8, (window.innerWidth / 2 - 170)) + 'px'; pop.style.top = '70px'; return; }
    var w = pop.offsetWidth || 320, h = pop.offsetHeight || 120;
    pop.style.left = Math.min(Math.max(8, rect.left), window.innerWidth - w - 8) + 'px';
    var top = rect.bottom + 8;
    if (top + h > window.innerHeight - 8) top = rect.top - h - 8;
    top = Math.max(54, Math.min(top, window.innerHeight - h - 8));   // 强制夹进视口(rect 为零尺寸页顶矩形/负 top 时不至于弹到屏外)
    pop.style.top = top + 'px';
  }
  function _ensurePop() {
    var p = document.getElementById('word-pop');
    if (!p) { p = document.createElement('div'); p.id = 'word-pop'; p.setAttribute('onclick', 'event.stopPropagation()'); document.body.appendChild(p); }
    // 关键:#word-pop 是单词框/词组框**共用**的同一元素。单词框(rc-wordpop 经 PDF _positionWordPop hook)会把它
    //   挪进 #main 并设 position:absolute(随内容滚动)。词组框走 _position 的 fixed 视口坐标 → 必须先还原成
    //   body + fixed,否则在滚动到深处的页面上,一个 absolute-in-#main 的元素按视口 top 定位会落到 #main 顶部
    //   (视口外几千 px)→ 表现为「框 show 了(disp=block)却看不见」,用户被迫再点一次 → 变成查单词。
    //   单词框那条路每次用时都会自己重设回 absolute-in-#main,是自愈的,不受本行影响。
    if (p.parentElement !== document.body) document.body.appendChild(p);
    p.style.position = 'fixed';
    return p;
  }
  // 框外点击 → 关小框(pointerdown capture;刚弹 400ms 内豁免)。rc-wordpop 已注册同款;本模块也注册一份
  // 以防本模块单独使用(两份都只是把 #word-pop 藏掉,幂等无害)。
  document.addEventListener('pointerdown', function (e) {
    var p = document.getElementById('word-pop');
    if (!p || p.style.display !== 'block') return;
    if (Date.now() - (window._wordPopOpenAt || 0) < 400) return;
    if (p.contains(e.target)) return;
    if (_ctx && _ctx.ignoreSelector && e.target && e.target.closest && e.target.closest(_ctx.ignoreSelector)) return;   // 点选中工具栏等豁免区不关框
    p.style.display = 'none';
  }, true);

  // ── 模块状态 ──
  var _state = null;   // {text, jp, reading, mastered}
  var _ctx = {};       // 当前 show 的 opts(底座回调 + langs)

  // 喇叭:读当前词组(日语念假名读音,否则有道/英语)。复用 rc-wordpop 暴露的 window._ttsWord / _speakOnline。
  window._epPhraseSpeak = function () {
    var s = _state; if (!s) return;
    if (s.reading && window._ttsWord) { window._ttsWord(s.reading, 'ja-JP'); return; }
    if (s.text && window._speakOnline) window._speakOnline(s.text);
  };
  // ☆ 收藏为词组(照搬 15-phrase-wordpop.js::_phraseFav):POST/DELETE /pdf/api/phrases。
  window._epPhraseFav = function (btn) {
    var s = _state; if (!s || !s.text) return;
    var t = s.text;
    var key = _norm(t);
    var has = _propertyValue(t, s.jp, 'favorite', _favSet.has(key));
    var nowFav = !has;
    _localTouched.favorite.add(key);
    // vocabulary-state 的 set 操作同步更新内存投影；持久化及旧服务器同步均不阻塞界面。
    _writeVocabularyState(t, s.jp, 'favorite', nowFav, 'phrasepop');
    _rememberFallback(t, 'favorite', nowFav);
    _paintFavButton(btn, nowFav);
    RC.toast(nowFav ? '已收藏，之后会作为一个词分词' : '已取消收藏');
    try { if (_ctx.onFav) _ctx.onFav(t, nowFav); } catch (_) {}
    _sendCompatibility('favorite', t, nowFav);
  };
  // ☆ 标记掌握(照搬 15-phrase-wordpop.js::_wordPopMaster 的 phrase 分支):POST /pdf/api/phrase-mark。
  // 现在与收藏相同，先同步写 vocabulary-state + 刷新底座；服务端仅在后台兼容同步。
  window._epPhraseMaster = function (btn) {
    var s = _state; if (!s) return;
    var t = s.text;
    var key = _norm(t);
    var current = _propertyValue(t, s.jp, 'mastered', s.mastered || _markSet.has(key));
    var next = !current;
    _localTouched.mastered.add(key);
    _writeVocabularyState(t, s.jp, 'mastered', next, 'phrasepop');
    _rememberFallback(t, 'mastered', next);
    s.mastered = next;
    _paintMasterButton(btn, next);
    RC.toast(next ? '已掌握，下划线消失' : '已取消掌握');
    try { if (_ctx.onMastered) _ctx.onMastered(t, next); } catch (_) {}
    _sendCompatibility('mastered', t, next);
  };
  // 💡 解释:藏框 → 底座 onExplain(走 RC.result 解释模态)。
  window._epPhraseExplain = function () {
    var p = document.getElementById('word-pop'); if (p) p.style.display = 'none';
    if (_ctx.onExplain) { try { _ctx.onExplain(_state && _state.text); } catch (_) {} }
  };

  // ── 入口:RC.phrasepop.show(opts)。逐字照搬 15-phrase-wordpop.js::showPhrasePopover 的内容/字段/端点 ──
  // opts.noDisplay=true → 只 fetch + 回调 onResult/onSolid,不弹框(查询期:词组高亮呼吸,结果存起来等点击才弹)。
  // opts.result={zh,reading,accent} → 用已存结果,不重新 fetch,秒开(点常亮高亮时用)。
  // opts.onResult(data) → fetch 完把 {text,zh,reading,accent} 回传(供底座存到高亮对象上)。
  function show(opts) {
    opts = opts || {};
    injectCss();
    _bindVocabularyState();
    var text = String(opts.text == null ? '' : opts.text).trim();
    if (!text) return;
    var isJa = _isJaWord(text);
    var context = String(opts.context || opts.ctx || opts.sentence || '').trim();
    if (!context) {
      try {
        var controller = window.__bwSelectionController;
        var selected = controller && controller.current && controller.current();
        if (selected && _norm(selected.text) === _norm(text)) {
          context = String(selected.context || selected.ctx || selected.sentence || '').trim();
        }
      } catch (_) {}
    }
    // 服务端实际只需邻近句境。限制 GET 长度，避免日文百分号编码后触发代理 URL 上限。
    context = context.slice(0, 320);
    var key = _norm(text);
    var noDisplay = !!opts.noDisplay;
    var pop = null;
    if (!noDisplay) {
      _ctx = opts;   // 只在真弹框时设(noDisplay 查询不渲染按钮,不该覆盖 _ctx → 防并发查询把已弹框按钮的回调覆盖丢失)
      pop = _ensurePop();
      _state = {
        text: text,
        jp: isJa,
        reading: '',
        mastered: _propertyValue(text, isJa, 'mastered', _markSet.has(key))
      };   // 掌握按钮按语言分流;词组掌握统一走 phrase-mark
      pop.style.display = 'block';
      window._wordPopOpenAt = Date.now();
      pop.innerHTML = '<div style="padding:14px;color:#8a9bb4">⏳ 处理词组…</div>';
      _position(pop, opts.rect);
    }
    var render = function (zh, reading, accent) {
      try { if (opts.onResult) opts.onResult({ text: text, zh: zh, reading: reading, accent: accent }); } catch (_) {}   // 用局部 opts(闭包)不读共享 _ctx:并发查询/点击不会互相覆盖回调
      try { if (opts.onSolid) opts.onSolid(); } catch (_) {}   // 出结果 → 底座停呼吸转常亮保持
      if (noDisplay || !pop) return;   // 查询模式:只回调,不渲染/不弹框
      _state.reading = reading;
      var phon = (isJa && reading && accent != null) ? _renderPitch(reading, accent)
        : (reading ? '<span class="wp-phon">' + esc(reading) + '</span>' : '');
      var fav = _propertyValue(text, isJa, 'favorite', _favSet.has(key));
      pop.innerHTML =
        '<div class="wp-head"><span class="wp-word">' + esc(text) + '</span>' + phon +
        (reading ? '<button class="wp-speak" onclick="_epPhraseSpeak()" title="发音">🔊</button>' : '') + '</div>' +
        '<div class="wp-def">' + (zh ? esc(zh) : '<span style="color:#8a9bb4">（无翻译）</span>') + '</div>' +
        '<div class="wp-actions">' +
        '<button id="ep-phrase-fav-btn" class="' + (fav ? 'wp-anki' : '') + '" onclick="_epPhraseFav(this)">' +
        (fav ? '★ 已收藏' : '☆ 收藏为词组') + '</button>' +
        '<button id="ep-phrase-master-btn" class="' + (_state.mastered ? 'wp-anki' : '') + '" onclick="_epPhraseMaster(this)" title="' + (_state.mastered ? '点击取消掌握（恢复词组下划线）' : '标记掌握 100（该词组不再标生词下划线）') + '">' +
        (_state.mastered ? '✓ 已掌握 100' : '☆ 标记掌握') + '</button>' +
        '<button onclick="_epPhraseExplain()" title="详细解释这个词组">💡 解释</button>' +
        '</div>';
      _position(pop, opts.rect);   // 内容定型后再夹一次进视口
    };
    if (opts.result) {   // 点击模式:用已存结果秒开,不重新 fetch
      var r = opts.result;
      render(r.zh || '', r.reading || '', (r.accent != null ? r.accent : null));
      return;
    }
    (async function () {
      var zh = '', reading = '', accent = null;
      try {
        if (isJa) {
          // @interaction dictionary.quick.read
          var dj = await (await fetch('/pdf/api/dict-jp?word=' + encodeURIComponent(text) +
            '&file=' + encodeURIComponent(opts.file || '') +
            '&page=' + encodeURIComponent(opts.page || 0) +
            '&langs=' + encodeURIComponent((opts.langs || []).join(',')) +
            '&context=' + encodeURIComponent(context))).json();
          if (dj && dj.ok) { zh = dj.zh || ''; reading = dj.reading || ''; accent = (dj.accent != null ? dj.accent : null); }
        }
        // 日语词组只接受结构化日中词典结果。词典后端失败时显示“无翻译”，不再把无句境
        // Google 结果（例如把 取り寄せ 译成“命令”）当作权威答案并写入长期缓存。
        if (!zh && !isJa) {
          // @interaction ai.translate.compute
          var dt = await (await fetch('/pdf/api/translate-sentence', {
            method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ text: text })
          })).json();
          if (dt && dt.ok) zh = dt.zh || '';
        }
      } catch (_) {}
      render(zh, reading, accent);
    })();
  }

  RC.phrasepop = { show: show, loadFavs: loadFavs, favList: favList };
  // vocabulary-state 可能由 runtime/扩展桥在本脚本之后完成 attach；ready 时重投影，
  // 不需要再次访问服务器。
  document.addEventListener('bw:vocabulary-state-ready', function () {
    _bindVocabularyState();
  });
  document.addEventListener('bw:reader-runtime-ready', function () {
    _bindVocabularyState();
  });
  loadFavs();
})();

})(window.__bwReaderDoc || document, window.__bwReaderFetch || window.fetch.bind(window));
