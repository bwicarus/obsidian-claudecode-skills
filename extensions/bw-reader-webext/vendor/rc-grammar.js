/* AUTO-GENERATED by build.py — 源=_server_deploy/static/pdf/rc-grammar.js 逐字包装,勿手改 */
;(function(document, fetch){
if (window.__bwPwaProviderOnly) return;
/* rc-grammar.js — 统一控制层(window.RC.grammar):语法分析(分词/依存句法/组成成分/骨架图)共享核心。
 *
 * 移植源:PDF 阅读器 reader.src/18-grammar.js(785 行,原生实现,保持基本不动,是 ?ui=legacy 的逃生口)。
 *   **逐字照搬其纯渲染逻辑**(_renderStructInto / _renderTree / _renderSkeleton / _renderComponents /
 *   _renderDepSvg / _renderDepTree / POS 配色 / 句子结构四模式),但把所有 PDF 专属全局量
 *   (FILE_REL / _grammarEnabledBooks / currentPage / _getAiOverrides() / 字符层选中)改成显式参数
 *   / opts 回调,让 EPUB(或将来别的 reader)可以直接调同一套函数,而不是各写一份。
 *
 * 参照的第二份先例(务必知道,别重蹈覆辙):`epub2-grammar.js`(epub.js 版 EPUB 阅读器的语法分析,982 行)
 *   已经把 18-grammar.js **逐字复制+改名**了一遍——正是本文件要避免的反模式(两边各一份、维护两次)。
 *   epub2-grammar.js 服务的 epub.js 阅读器正在退役(见 references/epub-reader-architecture.md),
 *   本次不动它;但它的 extractSentence/_splitSentences(EPUB 没有字符层,只能按标点切句抠出焦点所在句)
 *   被证明好用,原样吸收进本文件,供“主文档版”EPUB(epub-html.js)和将来任何无字符层的 reader 复用。
 *
 * 设计原则(跟 rc-md / rc-result / rc-knowledge / rc-sidedrawer 等共享层一致):
 *   · 自包含:自己注入一次 <style>(scoped 到调用方传入容器上加的 .rc-grammar class,不依赖任何模板 CSS);
 *   · opts/container 驱动:不假设固定 DOM id、不读宿主全局变量;“当前要分析的句子/焦点”由调用方算好传入
 *     (PDF 用字符层 _expandSentenceFromRange,EPUB 用本文件的 extractSentence 按标点切句)——即 host-bind 模式,
 *     跟 27-rc-adapter.js 喂 PdfAdapter 的方式一致,本文件不假设任何一方的选中机制。
 *   · 纯 DOM API + addEventListener 关闭包,不生成内联 onclick 字符串、不挂全局函数名 —— 这样跟 PDF 原生
 *     18-grammar.js 里的 window._grammarAnki / window._grammarFollowup / window.setGrammarView 等全局名
 *     **不会撞名**(PDF 共享模式下两份代码同时加载,若都用同名 window.* 后加载者会覆盖前者,见集成说明);
 *   · 用共享层 RC.esc / RC.md / RC.typeset / RC.toast / RC.reqJson / RC.result.aiStream(缺了有内联兜底);
 *   · 幂等:重复注入 CSS / 重复给容器加 scope class 不重复执行。
 *
 * API
 *   RC.grammar.extractSentence(ctx, focus) -> string
 *       纯函数,无 DOM 依赖。按句末标点(.!?。！？… + 收尾引号/括号)把 ctx 切句,返回包含 focus 的那一句
 *       (大小写不敏感);找不到 → focus 本身含句末标点则用 focus 截断;否则整 ctx 截断(退化)。
 *       给没有字符层的 reader(EPUB)从"该词/该选区所在段落文本"里抠出"该分析的整句"。
 *
 *   RC.grammar.loadTracked(file) -> Promise<{enabledBooks, hasTracked}>
 *       拉该书启用的语法 KG 列表 + 其中是否有节点被跟踪,内部缓存(单活跃文件语境,同 PDF/EPUB 一次只开一本书)。
 *   RC.grammar.enabledBooks() / RC.grammar.hasTracked() -> 读缓存,不发请求。
 *   RC.grammar.saveEnabledBooks(file, books) -> Promise   勾选变更后保存 + 刷新 hasTracked 缓存。
 *   RC.grammar.renderTrackList(container, opts) -> Promise
 *       把"启用哪些语法 KG"的勾选列表渲进 container(纯列表,无折叠头 chrome——折叠 UI 由宿主模板自己包一层,
 *       PDF 本来就是设置面板里的一个 tab、EPUB 包一层可折叠头,两边形状不同,不强行统一)。
 *       opts: {file, onAfterChange?}
 *
 *   RC.grammar.getViewMode(storageKey) -> string
 *   RC.grammar.setViewMode(container, mode, storageKey) -> void
 *       长句结构显示模式(树/块/主干/弧线),写 localStorage(key 由调用方给,PDF/EPUB 各自的历史键不同)+
 *       重渲染 container 内所有 .grammar-block 的结构图。
 *
 *   RC.grammar.analyze(opts) -> void   入口:分析一句话,结果块渲进 opts.container。
 *     opts: { file, sentence, text, container,
 *             aiParams()->{model,effort}?, sourceUrl()->string?, viewModeKey?,
 *             onOpenPanel()?, onToast(msg)?, onNeedSetup(kind)? }
 *     内部先查 enabledBooks/hasTracked 门槛(不足则 toast + onNeedSetup,不发起分析请求)。
 *
 *   RC.grammar.loadHistory(container, file, opts?) -> Promise
 *       该书历史分析记录(倒序,最新在上),渲进 container。opts 同 analyze 的 aiParams/sourceUrl/viewModeKey。
 *   RC.grammar.clearBlocks(container) -> void
 *
 * 后端路由全部复用 PDF 阅读器既有的 /pdf/api/grammar-*(analyze/stream/tracked/books/history[-save]/forget)——
 * 这些路由的 `file` 参数本来就是通用相对路径,PDF 和 epub2-grammar.js 已经在共用,不建任何新路由。
 */
(function () {
  'use strict';
  var RC = (window.RC = window.RC || {});
  if (RC.grammar) return;

  function esc(s) {
    if (RC.esc) return RC.esc(s);
    var d = document.createElement('div'); d.textContent = (s == null ? '' : String(s)); return d.innerHTML;
  }
  function md(s) { try { return RC.md ? RC.md(s) : esc(s); } catch (e) { return esc(s); } }
  function typeset(el) {
    try { if (RC.typeset) { RC.typeset(el); return; } } catch (e) {}
    try { if (el && window.MathJax && MathJax.typesetPromise) MathJax.typesetPromise([el]).catch(function () {}); } catch (e) {}
  }
  function toast(m) { try { if (RC.toast) RC.toast(m); } catch (e) {} }
  function aiStreamFn(url, o) {
    try { if (RC.result && RC.result.aiStream) return RC.result.aiStream(url, o); } catch (e) {}
    return Promise.resolve({ ok: false, text: '', error: 'RC.result 未加载' });
  }
  // 网络瞬断重试(幂等计算:切后台被掐→回前台自动重试,不重复副作用;对照 PDF __safeFetch / epub2 safeFetch)
  function safeFetch(url, init, opts) {
    opts = opts || {}; var retries = opts.retries || 0;
    return fetch(url, init).catch(function (e) {
      if (retries > 0) return new Promise(function (res) { setTimeout(res, 600); }).then(function () { return safeFetch(url, init, { retries: retries - 1 }); });
      throw e;
    });
  }
  function resolveContainer(c) {
    if (!c) return null;
    var el = (typeof c === 'string') ? document.getElementById(c) : c;
    if (el) ensureScope(el);
    return el;
  }

  // ════════════════════════════════════════════════════════════════════
  // CSS:一次性注入,scoped 到 .rc-grammar(由 ensureScope 加到调用方传入的容器上)。
  // 内容照搬 PDF pdf_reader.html 模板里 .grammar-block 系静态样式(经 epub2-grammar.js 验证过的等价版本)。
  // ════════════════════════════════════════════════════════════════════
  var _cssInjected = false;
  function injectCss() {
    if (_cssInjected) return; _cssInjected = true;
    var st = document.createElement('style'); st.id = 'rc-grammar-css';
    st.textContent =
      '.rc-grammar .grammar-block{position:relative;margin-bottom:12px;border:1px solid #2a3550;border-radius:8px;background:#0d1322;overflow:hidden}' +
      '.rc-grammar .grammar-block.focus{border-color:#fbbf24;box-shadow:0 0 0 1px rgba(251,191,36,.3)}' +
      '.rc-grammar .grammar-block .gb-header{display:flex;align-items:center;gap:7px;padding:10px 11px;cursor:pointer;user-select:none}' +
      '.rc-grammar .grammar-block .gb-header:hover{background:#141d33}' +
      '.rc-grammar .grammar-block .gb-header .gb-title{flex:1;min-width:0;font-size:12px;color:#cfe6ff;line-height:1.4;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}' +
      '.rc-grammar .grammar-block .gb-header .gb-badge{flex:0 0 auto;font-size:9px;color:#7dd3fc;background:#16243f;border-radius:8px;padding:1px 7px}' +
      '.rc-grammar .grammar-block .gb-header .gb-del{flex:0 0 auto;color:#5a6680;font-size:12px;cursor:pointer;padding:0 3px;opacity:.7}' +
      '.rc-grammar .grammar-block .gb-header .gb-del:hover{color:#ef4444;opacity:1}' +
      '.rc-grammar .grammar-block .gb-header .gb-caret{flex:0 0 auto;color:#7a8497;font-size:11px;transition:transform .15s}' +
      '.rc-grammar .grammar-block.open .gb-header .gb-caret{transform:rotate(90deg)}' +
      '.rc-grammar .grammar-block .gb-content{display:none}' +
      '.rc-grammar .grammar-block.open .gb-content{display:block}' +
      '.rc-grammar .grammar-block .gb-fu-answers{display:none;padding:0 12px}' +
      '.rc-grammar .grammar-block.open .gb-fu-answers{display:block}' +
      '.rc-grammar .grammar-block .gb-fu-q{margin-top:10px;padding-top:8px;border-top:1px solid #2a3550;color:#a8cdff;font-size:12px;font-weight:600}' +
      '.rc-grammar .grammar-block .gb-fu-a{margin-top:5px;color:#e6e6f0;font-size:12.5px;line-height:1.6}' +
      '.rc-grammar .grammar-block .gb-followup{display:none;gap:6px;padding:8px 12px 12px}' +
      '.rc-grammar .grammar-block.open .gb-followup{display:flex}' +
      '.rc-grammar .grammar-block .gb-fu-input{flex:1;min-width:0;background:#0d1322;border:1px solid #2a3550;color:#cfe6ff;border-radius:6px;padding:7px 10px;font-size:12px}' +
      '.rc-grammar .grammar-block .gb-fu-input:focus{outline:none;border-color:#3b6db5}' +
      '.rc-grammar .grammar-block .gb-followup button{background:#244470;border:1px solid #3b6db5;color:#fff;border-radius:6px;padding:7px 14px;cursor:pointer;font-size:12px;white-space:nowrap}' +
      '.rc-grammar .grammar-block .gb-followup button.gb-anki-btn{background:#13351f;border-color:#34d399;color:#7ee2b8;padding:7px 10px}' +
      '.rc-grammar .grammar-block .gb-trans{padding:11px 12px;font-size:12px;color:#cfe6ff;line-height:1.55;border-top:1px solid #1c2740}' +
      '.rc-grammar .grammar-block .gb-loading{padding:14px;text-align:center;color:#7a8497;font-style:italic;font-size:12px}' +
      '.rc-grammar .grammar-block .gb-pending{opacity:.65;font-style:italic;animation:rcGbPulse 1.2s ease-in-out infinite}' +
      '@keyframes rcGbPulse{0%,100%{opacity:.4}50%{opacity:.8}}' +
      '.rc-grammar .gb-diagram-wrap{border-top:1px solid #1c2740}' +
      '.rc-grammar .gb-diagram-toggle{display:flex;align-items:center;gap:6px;padding:9px 12px;cursor:pointer;font-size:11px;color:#9fb4d4;user-select:none}' +
      '.rc-grammar .gb-diagram-toggle:hover{background:#141d33}' +
      '.rc-grammar .gb-diagram-toggle .gv-switch{display:inline-flex;gap:2px;margin-left:8px}' +
      '.rc-grammar .gb-diagram-toggle .gv-switch button{background:#0d1322;border:1px solid #2a3550;color:#7a8497;font-size:10px;border-radius:4px;padding:1px 6px;cursor:pointer;line-height:1.5}' +
      '.rc-grammar .gb-diagram-toggle .gv-switch button:hover{color:#cfe6ff;border-color:#3b6db5}' +
      '.rc-grammar .gb-diagram-toggle .gv-switch button.active{background:#1a2540;color:#7dd3fc;border-color:#3b6db5;font-weight:600}' +
      '.rc-grammar .gb-diagram-toggle .dg-caret{margin-left:auto;color:#7a8497;font-size:10px;transition:transform .15s}' +
      '.rc-grammar .gb-diagram-wrap.dgram-open .gb-diagram-toggle .dg-caret{transform:rotate(90deg)}' +
      '.rc-grammar .grammar-block .gb-diagram{display:none;overflow-x:auto;overflow-y:hidden;padding:6px 10px 12px;-webkit-overflow-scrolling:touch}' +
      '.rc-grammar .gb-diagram-wrap.dgram-open .gb-diagram{display:block}' +
      '.rc-grammar .grammar-block .gb-diagram svg{display:block}' +
      '.rc-grammar .dep-clause{margin-bottom:12px}.rc-grammar .dep-clause:last-child{margin-bottom:0}' +
      '.rc-grammar .dep-clause-label{display:inline-block;font-size:10px;font-weight:600;color:#0d1322;background:#7dd3fc;border-radius:4px;padding:1px 7px;margin:0 0 3px 2px}' +
      '.rc-grammar .comp-blocks{display:flex;flex-direction:column;gap:5px;padding:2px 0}' +
      '.rc-grammar .comp-block{border-left:3px solid #64748b;border-radius:0 6px 6px 0;background:#0d1322;padding:6px 9px;line-height:1.5}' +
      '.rc-grammar .comp-block .comp-label{display:inline-block;font-size:9px;font-weight:600;color:#0d1322;border-radius:4px;padding:1px 7px;margin-right:7px;vertical-align:middle}' +
      '.rc-grammar .comp-block .comp-text{font-size:13px;color:#e6e6f0}' +
      '.rc-grammar .sk-blocks{font-size:14px;line-height:2;color:#e6e6f0;padding:2px 0}' +
      '.rc-grammar .sk-core{color:#e6e6f0}' +
      '.rc-grammar .sk-mod{display:inline-block;font-size:11px;border:1px dashed currentColor;border-radius:5px;padding:0 6px;margin:0 2px;cursor:pointer;opacity:.85;vertical-align:middle;white-space:normal}' +
      '.rc-grammar .sk-mod:hover{opacity:1;background:rgba(255,255,255,.06)}.rc-grammar .sk-mod.open{border-style:solid}' +
      '.rc-grammar .ctree{font-size:13px;padding:2px 0}' +
      '.rc-grammar .ctree .ct-row{display:flex;align-items:baseline;gap:6px;padding:3px 0}' +
      '.rc-grammar .ctree .ct-label{flex:0 0 auto;font-size:9px;font-weight:600;color:#0d1322;border-radius:4px;padding:1px 6px}' +
      '.rc-grammar .ctree .ct-text{color:#e6e6f0;line-height:1.45}' +
      '.rc-grammar .ctree .ct-caret{flex:0 0 auto;margin-left:auto;color:#7a8497;font-size:10px;cursor:pointer;padding:0 4px;user-select:none}' +
      '.rc-grammar .ctree .ct-children{margin-left:13px;border-left:1px dashed #2a3550;padding-left:9px}' +
      '.rc-grammar .ctree .ct-text.ct-core{color:#9fb4d4}' +
      '.rc-grammar .dep-token{cursor:pointer}.rc-grammar .dep-word{font-size:15px;font-weight:600}' +
      '.rc-grammar .dep-pos{font-size:9px;font-family:monospace;fill:#8a9bb4}' +
      '.rc-grammar .dep-arc{fill:none;stroke:#5a6e94;stroke-width:1.5}.rc-grammar .dep-arrow{fill:#5a6e94}' +
      '.rc-grammar .dep-label{font-size:9px;fill:#7dd3fc;font-weight:600}' +
      '.rc-grammar .dep-tree .dep-tscroll{overflow-x:auto;overflow-y:hidden;-webkit-overflow-scrolling:touch}' +
      '.rc-grammar .dep-tree .dep-tsub{margin:4px 0 8px 14px;border-left:1px dashed #2a3550;padding-left:9px}' +
      '.rc-grammar .dep-token.dep-ph .dep-word{text-decoration:underline dotted}' +
      '.rc-grammar .gb-analyses{border-top:1px solid #1c2740;padding:2px 0}' +
      '.rc-grammar .gb-ana{padding:9px 12px;border-bottom:1px solid #141c30;cursor:pointer}' +
      '.rc-grammar .gb-ana:last-child{border-bottom:none}' +
      '.rc-grammar .gb-ana .a-head{font-size:12px;font-weight:600;color:#a8cdff}' +
      '.rc-grammar .gb-ana .a-phrase{font-size:10px;color:#facc15;font-family:monospace;margin-top:3px}' +
      '.rc-grammar .gb-ana .a-body{display:none;font-size:11px;color:#cfe6ff;line-height:1.55;margin-top:6px}' +
      '.rc-grammar .gb-ana.open .a-body{display:block}' +
      '.rc-grammar .gb-ana .a-ex{margin:5px 0 0 16px;padding:0;font-size:10px;color:#7a8497;line-height:1.5}' +
      '.rc-grammar .gb-empty{color:#5a6680;font-size:12px;padding:18px 12px;text-align:center;line-height:1.6}' +
      '.rc-grammar .rc-gram-kg-empty{color:#7a8497;font-size:12px;padding:6px 2px;line-height:1.6}' +
      '.rc-grammar .rc-gram-kg-row{display:flex;align-items:center;gap:8px;padding:6px 4px;cursor:pointer;color:#cfe6ff;border-radius:3px;border-bottom:1px solid #1f2740}' +
      '#rc-grammar-dep-tip{position:fixed;z-index:9200;background:#1a2540;border:1px solid #3b6db5;border-radius:6px;padding:5px 9px;font-size:11px;color:#e6e6f0;pointer-events:none;max-width:220px;box-shadow:0 4px 14px rgba(0,0,0,.5);display:none}';
    document.head.appendChild(st);
  }
  var _scoped = (typeof WeakSet !== 'undefined') ? new WeakSet() : null;
  function ensureScope(el) {
    injectCss();
    if (_scoped) { if (_scoped.has(el)) return; _scoped.add(el); }
    if (el.classList) el.classList.add('rc-grammar');
  }

  // ════════════════════════════════════════════════════════════════════
  // POS 配色 + 中文短标签(displaCy 风格)—— 逐字照搬 18-grammar.js
  // ════════════════════════════════════════════════════════════════════
  var POS_COLORS = {
    noun: '#3b82f6', verb: '#ef4444', adj: '#22c55e', adv: '#a855f7',
    pron: '#ec4899', prep: '#06b6d4', det: '#64748b', conj: '#eab308',
    aux: '#f97316', num: '#14b8a6', part: '#8b5cf6', intj: '#f43f5e', punct: '#475569'
  };
  var POS_LABEL = {
    noun: '名', verb: '动', adj: '形', adv: '副', pron: '代', prep: '介',
    det: '限', conj: '连', aux: '助', num: '数', part: '小品', intj: '叹', punct: '标'
  };
  function posColor(p) { return POS_COLORS[(p || '').toLowerCase()] || '#64748b'; }
  function compColor(label) {
    label = label || '';
    if (label.indexOf('谓语') >= 0) return '#ef4444';
    if (label.indexOf('主语') >= 0) return '#3b82f6';
    if (label.indexOf('宾语') >= 0) return '#22c55e';
    if (label.indexOf('定语') >= 0) return '#06b6d4';
    if (label.indexOf('状语') >= 0) return '#a855f7';
    if (label.indexOf('表语') >= 0 || label.indexOf('补语') >= 0) return '#eab308';
    if (label.indexOf('并列') >= 0) return '#94a3b8';
    return '#8a9bb4';
  }

  // ── 依存图悬停提示:单个共享实例(懒建),不跟任何宿主的 #dep-tip / #ep-dep-tip 撞名 ──
  var _depTipEl = null;
  function ensureDepTip() {
    if (!_depTipEl) {
      _depTipEl = document.getElementById('rc-grammar-dep-tip');
      if (!_depTipEl) { _depTipEl = document.createElement('div'); _depTipEl.id = 'rc-grammar-dep-tip'; document.body.appendChild(_depTipEl); }
    }
    return _depTipEl;
  }
  function showDepTip(ev, text) { var el = ensureDepTip(); el.textContent = text; el.style.display = 'block'; moveDepTip(ev); }
  function moveDepTip(ev) { if (!_depTipEl) return; _depTipEl.style.left = ((ev.clientX || 0) + 12) + 'px'; _depTipEl.style.top = ((ev.clientY || 0) + 14) + 'px'; }
  function hideDepTip() { if (_depTipEl) _depTipEl.style.display = 'none'; }

  // ════════════════════════════════════════════════════════════════════
  // 结构渲染:成分树 / 主干折叠 / 成分分块 / displaCy 依存弧线图(可逐级展开子从句)
  // 逐字照搬 18-grammar.js 的 _renderTree/_renderSkeleton/_renderComponents/_renderDepSvg/_renderDepTree,
  // 只把 DOM 事件从"依赖模块作用域"改成纯闭包(无需改动逻辑本身)。
  // ════════════════════════════════════════════════════════════════════
  function renderSkeleton(comps) {
    var CORE = { '主语': 1, '谓语': 1, '宾语': 1, '间接宾语': 1, '表语': 1, '宾语补足语': 1, '形式主语': 1 };
    var wrap = document.createElement('div'); wrap.className = 'sk-blocks';
    for (var i = 0; i < comps.length; i++) {
      var c = comps[i], label = c.label || '';
      if (CORE[label]) {
        var s = document.createElement('span'); s.className = 'sk-core'; s.textContent = c.text || ''; wrap.appendChild(s);
      } else {
        var chip = document.createElement('span'); chip.className = 'sk-mod';
        chip.style.color = compColor(label); chip.dataset.text = c.text || ''; chip.dataset.label = label;
        chip.textContent = '[' + label + ' …]';
        (function (chip, label) {
          chip.addEventListener('click', function () {
            var open = chip.classList.toggle('open');
            chip.textContent = open ? ('[' + label + '：' + chip.dataset.text + ']') : ('[' + label + ' …]');
          });
        })(chip, label);
        wrap.appendChild(chip);
      }
      wrap.appendChild(document.createTextNode(' '));
    }
    return wrap;
  }
  function renderTree(comps) {
    var children = {};
    comps.forEach(function (c, i) { var p = (c.parent == null ? -1 : c.parent); (children[p] = children[p] || []).push(i); });
    var fullText = function (idx) {
      var acc = [];
      var collect = function (i) { acc.push(comps[i]); (children[i] || []).forEach(collect); };
      collect(idx);
      acc.sort(function (a, b) { return (a.start || 0) - (b.start || 0); });
      return acc.map(function (c) { return c.text || ''; }).join(' ');
    };
    var build = function (idx) {
      var c = comps[idx], col = compColor(c.label || '');
      var kids = children[idx] || [];
      var node = document.createElement('div'); node.className = 'ctree-node';
      var row = document.createElement('div'); row.className = 'ct-row';
      var txt = document.createElement('span'); txt.className = 'ct-text';
      txt.textContent = kids.length ? fullText(idx) : (c.text || '');
      row.innerHTML = '<span class="ct-label" style="background:' + col + '">' + esc(c.label || '') + '</span>';
      row.appendChild(txt); node.appendChild(row);
      var isClause = (c.label || '').indexOf('从句') >= 0;
      if (kids.length) {
        var car = document.createElement('span'); car.className = 'ct-caret'; car.textContent = '▸';
        row.appendChild(car);
        var box = document.createElement('div'); box.className = 'ct-children'; box.style.display = 'none';
        if (isClause && (c.text || '').trim()) {
          var pred = document.createElement('div'); pred.className = 'ctree-node';
          pred.innerHTML = '<div class="ct-row"><span class="ct-label" style="background:' + compColor('谓语') + '">谓语</span><span class="ct-text">' + esc(c.text) + '</span></div>';
          box.appendChild(pred);
        }
        kids.forEach(function (k) { box.appendChild(build(k)); });
        node.appendChild(box);
        var toggle = function (e) {
          e.stopPropagation();
          var open = box.style.display === 'none';
          box.style.display = open ? '' : 'none';
          car.textContent = open ? '▾' : '▸';
          txt.textContent = open ? (isClause ? '' : (c.text || '')) : fullText(idx);
          txt.classList.toggle('ct-core', open && !isClause);
        };
        car.addEventListener('click', toggle);
        txt.style.cursor = 'pointer';
        txt.addEventListener('click', toggle);
      }
      return node;
    };
    var wrap = document.createElement('div'); wrap.className = 'ctree';
    (children[-1] || []).forEach(function (i) { wrap.appendChild(build(i)); });
    return wrap;
  }
  function renderComponents(comps) {
    var wrap = document.createElement('div'); wrap.className = 'comp-blocks';
    for (var i = 0; i < comps.length; i++) {
      var c = comps[i], col = compColor(c.label || '');
      var b = document.createElement('div'); b.className = 'comp-block'; b.style.borderLeftColor = col;
      b.innerHTML = '<span class="comp-label" style="background:' + col + '">' + esc(c.label || '') + '</span><span class="comp-text">' + esc(c.text || '') + '</span>';
      wrap.appendChild(b);
    }
    return wrap;
  }
  function renderDepSvg(tokens, deps, onNodeClick) {
    var NS = 'http://www.w3.org/2000/svg';
    var PAD_X = 18, GAP = 40, FONT = 16, POS_GAP = 18;
    var ARC_BASE = 24, ARC_STEP = 19;
    var meas = document.createElement('canvas').getContext('2d');
    meas.font = '600 ' + FONT + 'px -apple-system,system-ui,sans-serif';
    var widths = tokens.map(function (t) { return Math.max(meas.measureText(t.text || '').width, 12); });
    var cx = []; var x = PAD_X;
    for (var i = 0; i < tokens.length; i++) { cx.push(x + widths[i] / 2); x += widths[i] + GAP; }
    var totalW = Math.max(x - GAP + PAD_X, 60);
    var arcs = deps.map(function (dp) { return Object.assign({}, dp, { span: Math.abs(dp.head - dp.child) }); }).sort(function (a, b) { return a.span - b.span; });
    var maxSpan = arcs.length ? Math.max.apply(null, arcs.map(function (a) { return a.span; })) : 1;
    var wordBaseY = ARC_BASE + maxSpan * ARC_STEP + 26;
    var svgH = wordBaseY + POS_GAP + 8;
    var svg = document.createElementNS(NS, 'svg');
    svg.setAttribute('width', totalW); svg.setAttribute('height', svgH);
    svg.setAttribute('viewBox', '0 0 ' + totalW + ' ' + svgH);
    var arcBottomY = wordBaseY - FONT - 4;
    for (var ai = 0; ai < arcs.length; ai++) {
      var a = arcs[ai];
      var x1 = cx[a.head], x2 = cx[a.child];
      var dir = x2 > x1 ? 1 : -1;
      var top = arcBottomY - (ARC_BASE + a.span * ARC_STEP);
      var sx = x1 + dir * 3, ex = x2 - dir * 3;
      var path = document.createElementNS(NS, 'path');
      path.setAttribute('d', 'M ' + sx + ' ' + arcBottomY + ' C ' + sx + ' ' + top + ', ' + ex + ' ' + top + ', ' + ex + ' ' + arcBottomY);
      path.setAttribute('class', 'dep-arc'); svg.appendChild(path);
      var arrow = document.createElementNS(NS, 'path');
      arrow.setAttribute('d', 'M ' + (ex - 3) + ' ' + (arcBottomY - 5) + ' L ' + (ex + 3) + ' ' + (arcBottomY - 5) + ' L ' + ex + ' ' + arcBottomY + ' Z');
      arrow.setAttribute('class', 'dep-arrow'); svg.appendChild(arrow);
      if (a.label) {
        var lbl = document.createElementNS(NS, 'text');
        lbl.setAttribute('x', (sx + ex) / 2); lbl.setAttribute('y', top - 2);
        lbl.setAttribute('text-anchor', 'middle'); lbl.setAttribute('class', 'dep-label');
        lbl.textContent = a.label; svg.appendChild(lbl);
      }
    }
    for (var ti = 0; ti < tokens.length; ti++) {
      var t = tokens[ti];
      var isClause = (t.pos === 'clause') || (t.ref != null);
      var col = isClause ? '#7dd3fc' : posColor(t.pos);
      var g = document.createElementNS(NS, 'g');
      g.setAttribute('class', 'dep-token' + (isClause ? ' dep-ph' : ''));
      var bw = widths[ti] + 8;
      var bg = document.createElementNS(NS, 'rect');
      bg.setAttribute('x', cx[ti] - bw / 2); bg.setAttribute('y', wordBaseY - FONT);
      bg.setAttribute('width', bw); bg.setAttribute('height', FONT + 5);
      bg.setAttribute('rx', 4); bg.setAttribute('fill', col); bg.setAttribute('opacity', isClause ? '0.22' : '0.16');
      if (isClause) { bg.setAttribute('stroke', col); bg.setAttribute('stroke-dasharray', '3 2'); bg.setAttribute('stroke-width', '1'); }
      g.appendChild(bg);
      var w = document.createElementNS(NS, 'text');
      w.setAttribute('x', cx[ti]); w.setAttribute('y', wordBaseY - 2);
      w.setAttribute('text-anchor', 'middle'); w.setAttribute('class', 'dep-word'); w.setAttribute('fill', col);
      w.textContent = t.text || ''; g.appendChild(w);
      var p = document.createElementNS(NS, 'text');
      p.setAttribute('x', cx[ti]); p.setAttribute('y', wordBaseY + POS_GAP - 2);
      p.setAttribute('text-anchor', 'middle'); p.setAttribute('class', 'dep-pos');
      p.textContent = isClause ? '点开▾' : (POS_LABEL[(t.pos || '').toLowerCase()] || t.pos || '');
      g.appendChild(p);
      if (isClause && onNodeClick) {
        g.style.cursor = 'pointer';
        (function (ti, g) { g.addEventListener('click', function (ev) { ev.stopPropagation(); onNodeClick(ti, g); }); })(ti, g);
      } else if (t.zh) {
        var tip = (t.text || '') + '　' + t.zh;
        (function (tip) {
          g.addEventListener('mouseenter', function (ev) { showDepTip(ev, tip); });
          g.addEventListener('mousemove', moveDepTip);
          g.addEventListener('mouseleave', hideDepTip);
          g.addEventListener('click', function (ev) { showDepTip(ev, tip); setTimeout(hideDepTip, 2600); });
        })(tip);
      }
      svg.appendChild(g);
    }
    return svg;
  }
  function renderDepTree(tree) {
    var wrap = document.createElement('div'); wrap.className = 'dep-tree';
    var renderClause = function (clause, container) {
      var seg = document.createElement('div'); seg.className = 'dep-tlevel';
      var childBox = document.createElement('div'); childBox.className = 'dep-tchildren';
      var svg = renderDepSvg(clause.nodes || [], clause.deps || [], function (i) {
        var node = (clause.nodes || [])[i];
        if (!node || node.ref == null) return;
        var exist = childBox.querySelector(':scope > [data-ref="' + node.ref + '"]');
        if (exist) { exist.remove(); return; }
        var sub = document.createElement('div'); sub.dataset.ref = node.ref; sub.className = 'dep-tsub';
        var lbl = document.createElement('div'); lbl.className = 'dep-clause-label';
        lbl.textContent = (clause.children[node.ref] || {}).label || '从句';
        sub.appendChild(lbl);
        renderClause(clause.children[node.ref], sub);
        childBox.appendChild(sub);
      });
      var sc = document.createElement('div'); sc.className = 'dep-tscroll'; sc.appendChild(svg);
      sc.addEventListener('wheel', function (e) {
        if (sc.scrollWidth <= sc.clientWidth) return;
        sc.scrollLeft += (Math.abs(e.deltaY) > Math.abs(e.deltaX) ? e.deltaY : e.deltaX);
        e.preventDefault();
      }, { passive: false });
      seg.appendChild(sc); seg.appendChild(childBox); container.appendChild(seg);
    };
    renderClause(tree, wrap);
    return wrap;
  }

  // ── 长句结构显示模式(树/块/主干/弧线):localStorage key 由调用方给(PDF/EPUB 各自历史键不同)──
  var GV_MODES = [['tree', '树'], ['components', '块'], ['skeleton', '主干'], ['deps', '弧线']];
  var DEFAULT_VIEW_KEY = 'rc-grammar-view';
  function getViewMode(storageKey) {
    var k = storageKey || DEFAULT_VIEW_KEY;
    var v = null; try { v = localStorage.getItem(k); } catch (e) {}
    return ['deps', 'skeleton', 'components', 'tree'].indexOf(v) >= 0 ? v : 'components';
  }
  function setViewMode(container, mode, storageKey) {
    var k = storageKey || DEFAULT_VIEW_KEY;
    mode = ['deps', 'skeleton', 'components', 'tree'].indexOf(mode) >= 0 ? mode : 'components';
    try { localStorage.setItem(k, mode); } catch (e) {}
    container = resolveContainer(container);
    if (!container) return;
    var blocks = container.querySelectorAll('.grammar-block');
    for (var i = 0; i < blocks.length; i++) {
      var b = blocks[i]; if (!b.__spacy) continue;
      var diag = b.querySelector('.gb-diagram'), wrap = b.querySelector('.gb-diagram-wrap');
      if (diag) renderStructInto(diag, wrap, b.__spacy, mode);
    }
    var btns = container.querySelectorAll('.gv-switch button');
    for (var j = 0; j < btns.length; j++) btns[j].classList.toggle('active', btns[j].dataset.gv === mode);
  }
  function renderStructInto(diag, wrap, sp, viewMode) {
    diag.innerHTML = '';
    var comps = sp.components || [], toks = sp.tokens || [], deps = sp.deps || [], clauses = sp.clauses || [];
    if (viewMode === 'tree' && comps.length) {
      diag.appendChild(renderTree(comps)); if (wrap) wrap.classList.add('dgram-open');
    } else if (viewMode === 'skeleton' && comps.length) {
      diag.appendChild(renderSkeleton(comps)); if (wrap) wrap.classList.add('dgram-open');
    } else if (viewMode === 'components' && comps.length) {
      diag.appendChild(renderComponents(comps)); if (wrap) wrap.classList.add('dgram-open');
    } else if (sp.clause_tree) {
      diag.appendChild(renderDepTree(sp.clause_tree));
    } else if (toks.length) {
      if (clauses.length > 1) {
        for (var ci = 0; ci < clauses.length; ci++) {
          var c = clauses[ci];
          if (!(c.tokens || []).length) continue;
          var seg = document.createElement('div'); seg.className = 'dep-clause';
          var lbl = document.createElement('div'); lbl.className = 'dep-clause-label'; lbl.textContent = c.label || '从句';
          seg.appendChild(lbl); seg.appendChild(renderDepSvg(c.tokens, c.deps || [])); diag.appendChild(seg);
        }
      } else {
        diag.appendChild(renderDepSvg(toks, deps));
      }
      diag.addEventListener('wheel', function (e) {
        if (diag.scrollWidth <= diag.clientWidth) return;
        var dy = Math.abs(e.deltaY) > Math.abs(e.deltaX) ? e.deltaY : e.deltaX;
        diag.scrollLeft += dy; e.preventDefault();
      }, { passive: false });
    }
  }

  // ════════════════════════════════════════════════════════════════════
  // 句子抽取(EPUB 没有字符层,靠标点切句):吸收自 epub2-grammar.js::extractSentence(已验证好用)。
  // ════════════════════════════════════════════════════════════════════
  function splitSentences(text) {
    var ENDERS = '.!?。！？…';
    var CLOSERS = '”’"\'）)」』】>';
    var out = [], cur = '';
    for (var i = 0; i < text.length; i++) {
      var ch = text[i];
      if (ch === '\n' || ch === '\r') { if (cur.trim()) out.push(cur); cur = ''; continue; }
      cur += ch;
      if (ENDERS.indexOf(ch) >= 0) {
        while (i + 1 < text.length && CLOSERS.indexOf(text[i + 1]) >= 0) { cur += text[++i]; }
        out.push(cur); cur = '';
      }
    }
    if (cur.trim()) out.push(cur);
    return out.map(function (s) { return s.trim(); }).filter(Boolean);
  }
  function extractSentence(ctx, focus) {
    ctx = String(ctx == null ? '' : ctx).replace(/\s+/g, ' ').trim();
    focus = String(focus == null ? '' : focus).replace(/\s+/g, ' ').trim();
    if (!ctx) return focus;
    var sents = splitSentences(ctx);
    if (focus) {
      var fl = focus.toLowerCase();
      for (var i = 0; i < sents.length; i++) { if (sents[i].toLowerCase().indexOf(fl) >= 0) return sents[i]; }
      if (/[.!?。！？…]/.test(focus)) return focus.slice(0, 400);
    }
    if (sents.length === 1) return sents[0];
    return ctx.slice(0, 400);
  }

  // ════════════════════════════════════════════════════════════════════
  // 后台任务小浮条(🎴 Anki 制卡用):自带一份(RC.result 的同名私有函数未导出),
  // 复用 #bg-jobs 容器 id → 跟宿主其它后台任务视觉上堆叠一致。
  // ════════════════════════════════════════════════════════════════════
  var _bgSeq = 0;
  function ensureBgJobsEl() {
    var c = document.getElementById('bg-jobs');
    if (!c) {
      c = document.createElement('div'); c.id = 'bg-jobs';
      c.style.cssText = 'position:fixed;right:18px;bottom:80px;display:flex;flex-direction:column;gap:6px;z-index:520;align-items:flex-end';
      document.body.appendChild(c);
    }
    return c;
  }
  function startBgJob(text) {
    var id = 'rcgj' + (++_bgSeq);
    var el = document.createElement('div'); el.id = id;
    el.style.cssText = 'background:#10162a;border:1px solid #3b6db5;color:#cfe6ff;padding:7px 12px;border-radius:8px;font-size:12px;box-shadow:0 4px 12px rgba(0,0,0,.5);max-width:280px';
    el.textContent = '⏳ ' + text;
    ensureBgJobsEl().appendChild(el);
    return id;
  }
  function finishBgJob(id, text, url) {
    var el = document.getElementById(id); if (!el) return;
    el.style.borderColor = '#34d399'; el.style.color = '#34d399';
    el.textContent = '✓ ' + text + (url ? ' · 点击打开' : '');
    if (url) { el.style.cursor = 'pointer'; el.onclick = function () { location.href = url; }; }
    setTimeout(function () { el.style.transition = 'opacity .4s'; el.style.opacity = '0'; setTimeout(function () { el.remove(); }, 400); }, url ? 8000 : 4500);
  }
  function failBgJob(id, text) {
    var el = document.getElementById(id); if (!el) return;
    el.style.borderColor = '#f87171'; el.style.color = '#f87171'; el.style.cursor = 'pointer';
    el.textContent = '✗ ' + text + ' · 点关闭';
    el.onclick = function () { el.remove(); };
  }
  function pollBgJob(jobId, ui) {
    var tries = 0, unknown = 0;
    var iv = setInterval(function () {
      tries++;
      fetch('/pdf/api/job-status?id=' + encodeURIComponent(jobId)).then(function (r) { return r.json(); }).then(function (d) {
        if (d.status === 'done') {
          clearInterval(iv);
          var out = d.result || {};
          if (out.ok) {
            var parts = [];
            if (out.note_path) parts.push('笔记已建');
            if (out.anki_added) parts.push('Anki ' + out.anki_added + ' 张');
            finishBgJob(ui, parts.join(' · ') || '完成', out.obsidian_url || '');
          } else { failBgJob(ui, out.error || '失败'); }
        } else if (d.status === 'error') {
          clearInterval(iv); failBgJob(ui, d.error || '失败');
        } else if (d.status === 'unknown') {
          if (++unknown >= 3) { clearInterval(iv); failBgJob(ui, '任务丢失(服务重启?)'); }
        }
      }).catch(function () { if (tries > 180) { clearInterval(iv); failBgJob(ui, '轮询超时'); } });
    }, 2000);
  }

  // ════════════════════════════════════════════════════════════════════
  // 启用语法 KG(per-file)+ 跟踪状态:单活跃文件语境(PDF/EPUB 一次只开一本书,跟原实现一致)。
  // ════════════════════════════════════════════════════════════════════
  var _enabledBooks = [];
  var _hasTracked = false;
  var _loaded = false;
  function refreshHasTracked() {
    if (!_enabledBooks.length) { _hasTracked = false; return Promise.resolve(); }
    return fetch('/pdf/api/grammar-books').then(function (r) { return r.json(); }).then(function (d) {
      var set = {}; _enabledBooks.forEach(function (b) { set[b] = 1; });
      _hasTracked = ((d && d.books) || []).some(function (b) { return set[b.book] && (b.tracked_count || 0) > 0; });
    }).catch(function () { _hasTracked = false; });
  }
  function loadTracked(file) {
    return fetch('/pdf/api/grammar-tracked?file=' + encodeURIComponent(file || '')).then(function (r) { return r.json(); })
      .then(function (d) { _enabledBooks = (d && d.enabled_books) || []; })
      .catch(function () { _enabledBooks = []; })
      .then(refreshHasTracked)
      .then(function () { _loaded = true; return { enabledBooks: _enabledBooks, hasTracked: _hasTracked }; });
  }
  function saveEnabledBooks(file, books) {
    _enabledBooks = books || [];
    return fetch('/pdf/api/grammar-tracked', {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ file: file || '', enabled_books: _enabledBooks })
    }).catch(function () {}).then(refreshHasTracked);
  }
  function renderTrackList(container, opts) {
    opts = opts || {};
    container = resolveContainer(container);
    if (!container) return Promise.resolve();
    var file = opts.file || '';
    container.innerHTML = '<div class="rc-gram-kg-empty">加载中…</div>';
    return fetch('/pdf/api/grammar-books').then(function (r) { return r.json(); }).then(function (d) {
      var books = (d && d.books) || [];
      return loadTracked(file).then(function () {
        var enabledSet = {}; _enabledBooks.forEach(function (b) { enabledSet[b] = 1; });
        if (!books.length) {
          container.innerHTML = '<div class="rc-gram-kg-empty">还没有语法 KG。新建一本 kind=grammar 的书后会出现。</div>';
          return;
        }
        container.innerHTML = '';
        books.forEach(function (b) {
          var row = document.createElement('label'); row.className = 'rc-gram-kg-row';
          row.title = '共 ' + b.total_l2 + ' 个 level-2 语法点';
          var cb = document.createElement('input'); cb.type = 'checkbox'; cb.value = b.book; cb.style.margin = '0';
          cb.checked = !!enabledSet[b.book];
          var info = document.createElement('div'); info.style.cssText = 'flex:1;min-width:0';
          var hotHtml = (b.tracked_count > 0)
            ? '<span style="color:#34d399;margin-left:4px">' + b.tracked_count + ' 已跟踪</span>'
            : '<span style="color:#7a8497;margin-left:4px">无跟踪（去技能树点节点开）</span>';
          info.innerHTML = '<div style="font-size:12px">' + esc(b.title) + '</div>' +
            '<div style="font-size:10px;color:#7a8497">' + b.total_l2 + ' 个语法点 · ' + hotHtml + '</div>';
          var link = document.createElement('a');
          link.href = '/skilltree/' + encodeURIComponent(b.book) + '/'; link.target = '_blank';
          link.style.cssText = 'color:#60a5fa;font-size:11px;text-decoration:none';
          link.textContent = '技能树 →';
          link.addEventListener('click', function (e) { e.stopPropagation(); });
          row.appendChild(cb); row.appendChild(info); row.appendChild(link);
          cb.addEventListener('change', function () {
            var checked = [];
            var boxes = container.querySelectorAll('input[type=checkbox]');
            for (var i = 0; i < boxes.length; i++) if (boxes[i].checked) checked.push(boxes[i].value);
            saveEnabledBooks(file, checked).then(function () { try { opts.onAfterChange && opts.onAfterChange(); } catch (e) {} });
          });
          container.appendChild(row);
        });
      });
    }).catch(function () {});
  }

  // ════════════════════════════════════════════════════════════════════
  // 分析块:标题栏(可折叠)+ 常驻翻译行 + 内容区(结构图+语法点)+ 追问/制卡。
  // 逐字照搬 18-grammar.js 的 DOM 结构/class 名(视觉零回归),交互从内联 onclick 改成
  // 闭包 addEventListener(不生成任何 window.* 全局名,PDF 共享模式下跟原生 18-grammar.js 不撞名)。
  // ════════════════════════════════════════════════════════════════════
  function _skeletonHtml(id, sentence, pending) {
    var summary = sentence.slice(0, 60) + (sentence.length > 60 ? '…' : '');
    return '<div class="gb-header">' +
      '<span class="gb-title" title="' + esc(sentence) + '">' + esc(summary) + '</span>' +
      '<span class="gb-del" title="删除这条（同句下次重新分析）">🗑</span>' +
      '<span class="gb-caret">▶</span></div>' +
      '<div class="gb-trans' + (pending ? ' gb-pending' : '') + '">' + (pending ? '🌐 翻译中…' : '') + '</div>' +
      '<div class="gb-content">' + (pending ? '<div class="gb-loading">⏳ 结构 / 语法分析中…</div>' : '') + '</div>' +
      '<div class="gb-fu-answers"></div>' +
      '<div class="gb-followup">' +
      '<input class="gb-fu-input" placeholder="继续追问这句的语法…">' +
      '<button class="gb-fu-btn">追问</button>' +
      '<button class="gb-anki-btn" title="整句+译文+分析做成 Anki 卡">🎴</button></div>';
  }
  function _wireBlockChrome(block, spec) {
    block.querySelector('.gb-header').addEventListener('click', function () { block.classList.toggle('open'); });
    block.querySelector('.gb-del').addEventListener('click', function (e) {
      e.stopPropagation();
      fetch('/pdf/api/grammar-forget', {
        method: 'POST', headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ sentence: block.dataset.src || '', text: block.dataset.text || '', file: spec.file || '', enabled_books: _enabledBooks })
      }).catch(function () {});
      block.remove();
    });
    var inp = block.querySelector('.gb-fu-input');
    inp.addEventListener('keydown', function (e) { if (e.key === 'Enter') { e.preventDefault(); _followup(block, spec); } });
    block.querySelector('.gb-fu-btn').addEventListener('click', function () { _followup(block, spec); });
    block.querySelector('.gb-anki-btn').addEventListener('click', function () { _anki(block, spec); });
  }
  function addLoadingBlock(container, spec) {
    container = resolveContainer(container);
    if (!container) return null;
    spec = spec || {};
    var ph = container.querySelector('.gb-empty'); if (ph) ph.remove();
    var id = 'gb_' + Date.now() + '_' + Math.random().toString(36).slice(2, 6);
    var block = document.createElement('div');
    block.className = 'grammar-block focus open';
    block.id = id;
    block.dataset.src = spec.sentence || '';
    block.dataset.text = spec.text || '';
    block.innerHTML = _skeletonHtml(id, spec.sentence || '', true);
    _wireBlockChrome(block, spec);
    var oldDup = container.querySelectorAll('.grammar-block');
    for (var i = 0; i < oldDup.length; i++) { if (oldDup[i] !== block && oldDup[i].dataset.src === spec.sentence) oldDup[i].remove(); }
    container.insertBefore(block, container.firstChild);
    container.scrollTop = 0;
    return block;
  }
  function fillBlockError(block, msg) {
    if (!block) return;
    var content = block.querySelector('.gb-content') || block;
    content.innerHTML = '<div class="gb-loading" style="color:#ef4444">分析失败：' + esc(msg) + '</div>';
  }
  function setBlockTrans(block, zh) {
    block.__zh = zh;
    var el = block.querySelector('.gb-trans');
    if (el) { el.classList.remove('gb-pending'); el.textContent = '🌐 ' + zh; }
  }
  function setBlockTransFail(block) {
    var el = block.querySelector('.gb-trans');
    if (el) { el.classList.remove('gb-pending'); el.textContent = '🌐 （翻译失败，可重试）'; }
  }
  function setBlockPoints(block, jsonStr, viewModeKey) {
    var wrap = block.querySelector('.gb-analyses');
    if (!wrap) return;
    var arr = []; try { arr = JSON.parse(jsonStr) || []; } catch (e) { arr = []; }
    block.__points = arr;
    if (!arr.length) { wrap.remove(); return; }
    wrap.classList.remove('gb-pending');
    wrap.innerHTML = arr.map(function (a) {
      return '<div class="gb-ana"><div class="a-head">📊 ' + esc(a.point || a.node_name || '') + '</div>' +
        (a.phrase ? '<div class="a-phrase">📍 ' + esc(a.phrase) + '</div>' : '') +
        '<div class="a-body">' + esc(a.explanation || '') + ((a.examples || []).length ? '<ul class="a-ex">' + (a.examples || []).map(function (e) { return '<li>' + esc(e) + '</li>'; }).join('') + '</ul>' : '') + '</div></div>';
    }).join('');
    var anaEls = wrap.querySelectorAll('.gb-ana');
    for (var i = 0; i < anaEls.length; i++) (function (el) { el.addEventListener('click', function () { el.classList.toggle('open'); }); })(anaEls[i]);
    var headEl = block.querySelector('.gb-header');
    if (headEl && !headEl.querySelector('.gb-badge')) {
      var badge = document.createElement('span'); badge.className = 'gb-badge'; badge.textContent = '语法点 ' + arr.length;
      headEl.insertBefore(badge, headEl.querySelector('.gb-del'));
    }
  }
  function fillBlock(block, d, sentence, viewModeKey) {
    if (!block) return;
    var tokens = d.tokens || [], deps = d.deps || [], sentence_zh = d.sentence_zh || '';
    var analyses = d.analyses || [], components = d.components || [];
    if (!tokens.length && !sentence_zh && !analyses.length && !components.length) {
      fillBlockError(block, d.raw ? 'AI 未返回结构化结果' : '无结果'); return;
    }
    block.__spacy = { tokens: tokens, deps: deps, clauses: d.clauses || [], components: components, clause_tree: d.clause_tree };
    var isSpacy = d.engine === 'spacy';
    var transEl = block.querySelector('.gb-trans');
    if (transEl) {
      if (sentence_zh) { transEl.classList.remove('gb-pending'); transEl.textContent = '🌐 ' + sentence_zh; }
      else if (!isSpacy) { transEl.remove(); }
    }
    var headEl = block.querySelector('.gb-header');
    if (headEl && analyses.length && !headEl.querySelector('.gb-badge')) {
      var badge = document.createElement('span'); badge.className = 'gb-badge'; badge.textContent = '语法点 ' + analyses.length;
      headEl.insertBefore(badge, headEl.querySelector('.gb-del'));
    }
    var hasStruct = components.length || tokens.length;
    var viewMode = getViewMode(viewModeKey);
    var gvHtml = GV_MODES.map(function (m) { return '<button type="button" data-gv="' + m[0] + '" class="' + (m[0] === viewMode ? 'active' : '') + '">' + m[1] + '</button>'; }).join('');
    var diagramHtml = hasStruct
      ? '<div class="gb-diagram-wrap"><div class="gb-diagram-toggle">📐 句子结构<span class="gv-switch">' + gvHtml + '</span><span class="dg-caret">▶</span></div><div class="gb-diagram"></div></div>'
      : '';
    var anaHtml = analyses.length
      ? '<div class="gb-analyses">' + analyses.map(function (a, i) {
          return '<div class="gb-ana" data-i="' + i + '"><div class="a-head">📊 ' + esc(a.node_name || a.node_id || '') + '</div>' +
            (a.phrase ? '<div class="a-phrase">📍 ' + esc(a.phrase) + '</div>' : '') +
            '<div class="a-body">' + esc(a.explanation || '') + ((a.examples || []).length ? '<ul class="a-ex">' + (a.examples || []).map(function (e) { return '<li>' + esc(e) + '</li>'; }).join('') + '</ul>' : '') + '</div></div>';
        }).join('') + '</div>'
      : (isSpacy ? '<div class="gb-analyses gb-pending"><div class="gb-ana"><div class="a-head" style="color:#7a8497;font-weight:400">⏳ 语法点分析中…</div></div></div>' : '');
    var content = block.querySelector('.gb-content');
    content.innerHTML = diagramHtml + anaHtml;
    if (hasStruct) {
      var wrap = content.querySelector('.gb-diagram-wrap');
      var diag = content.querySelector('.gb-diagram');
      renderStructInto(diag, wrap, block.__spacy, viewMode);
      content.querySelector('.gb-diagram-toggle').addEventListener('click', function () { wrap.classList.toggle('dgram-open'); });
      var gvBtns = content.querySelectorAll('.gv-switch button');
      for (var i = 0; i < gvBtns.length; i++) {
        (function (btn) {
          btn.addEventListener('click', function (e) {
            e.stopPropagation();
            setViewMode(block.parentElement, btn.dataset.gv, viewModeKey);
          });
        })(gvBtns[i]);
      }
    }
    var anaEls = content.querySelectorAll('.gb-ana');
    for (var j = 0; j < anaEls.length; j++) (function (el) { el.addEventListener('click', function () { el.classList.toggle('open'); }); })(anaEls[j]);
    setTimeout(function () { block.classList.remove('focus'); }, 5000);
  }
  function addHistoryBlock(container, item, spec) {
    container = resolveContainer(container);
    if (!container) return null;
    spec = spec || {};
    var ph = container.querySelector('.gb-empty'); if (ph) ph.remove();
    var sentence = item.sentence || '', text = item.text || '';
    var block = document.createElement('div');
    block.className = 'grammar-block';   // 历史卡默认折叠
    var hid = 'gbh_' + Date.now() + '_' + Math.random().toString(36).slice(2, 6);
    block.id = hid; block.dataset.src = sentence; block.dataset.text = text;
    block.innerHTML = _skeletonHtml(hid, sentence, false);
    _wireBlockChrome(block, spec);
    container.appendChild(block);
    fillBlock(block, item, sentence, spec.viewModeKey);   // item 无 engine → 直接渲染(不流式)
    return block;
  }
  function clearBlocks(container) {
    container = resolveContainer(container);
    if (!container) return;
    var blocks = container.querySelectorAll('.grammar-block');
    for (var i = 0; i < blocks.length; i++) blocks[i].remove();
  }

  // ── AI 流式:先收翻译([[TRANS]]先出→立刻显示),再收语法点([[POINTS]] JSON)──
  function streamGrammar(block, spec) {
    if (!block) return;
    var acc = '', transDone = false, pointsDone = false;
    var tryParse = function () {
      if (!transDone) {
        var tm = acc.match(/\[\[TRANS\]\]([\s\S]*?)\[\[\/TRANS\]\]/);
        if (tm) { setBlockTrans(block, tm[1].trim()); transDone = true; }
      }
      if (!pointsDone) {
        var pm = acc.match(/\[\[POINTS\]\]([\s\S]*?)\[\[\/POINTS\]\]/);
        if (pm) { setBlockPoints(block, pm[1].trim(), spec.viewModeKey); pointsDone = true; }
      }
    };
    var ap = (spec.aiParams ? (function () { try { return spec.aiParams() || {}; } catch (e) { return {}; } })() : {});
    return aiStreamFn('/pdf/api/grammar-stream', {
      method: 'POST',
      body: { sentence: spec.sentence, text: spec.text, file: spec.file || '', enabled_books: _enabledBooks, model: ap.model || '', effort: ap.effort || '' },
      onText: function (t) { acc = t; tryParse(); }
    }).then(function (res) {
      if (!res.ok && !acc) { setBlockTransFail(block); return; }
      acc = res.text || acc; tryParse();
      if (!transDone) {
        var tm = acc.match(/\[\[TRANS\]\]([\s\S]*?)(\[\[\/TRANS\]\]|\[\[POINTS\]\]|$)/);
        if (tm && tm[1].trim()) setBlockTrans(block, tm[1].trim()); else setBlockTransFail(block);
      }
      if (!pointsDone) {
        var pm = acc.match(/\[\[POINTS\]\]([\s\S]*?)(\[\[\/POINTS\]\]|$)/);
        setBlockPoints(block, pm ? pm[1].trim() : '[]', spec.viewModeKey);
      }
      try {
        var sp = block.__spacy || {};
        fetch('/pdf/api/grammar-history-save', {
          method: 'POST', headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ file: spec.file || '', item: {
            sentence: spec.sentence, text: spec.text, sentence_zh: block.__zh || '',
            tokens: sp.tokens || [], deps: sp.deps || [], clauses: sp.clauses || [],
            components: sp.components || [], clause_tree: sp.clause_tree || null,
            analyses: block.__points || []
          } })
        }).catch(function () {});
      } catch (e) {}
    });
  }

  // ── 🎴 整句+译文+分析+追问 → 一张 Anki 卡(后台 job + 浮条进度)──
  function _anki(block, spec) {
    var sentence = block.dataset.src || '';
    var zh = (block.querySelector('.gb-trans') ? block.querySelector('.gb-trans').textContent : '').replace(/^🌐\s*/, '').trim();
    var analysis = (block.querySelector('.gb-content') ? block.querySelector('.gb-content').textContent : '').trim();
    var fu = (block.querySelector('.gb-fu-answers') ? block.querySelector('.gb-fu-answers').textContent : '').trim();
    if (!sentence && !analysis) { toast('没有可制卡的内容'); return; }
    var srcUrl = ''; try { if (spec.sourceUrl) srcUrl = spec.sourceUrl() || ''; } catch (e) {}
    var text = '【句子】' + sentence + (zh ? '\n【译文】' + zh : '') + (analysis ? '\n【语法分析】' + analysis : '') +
      (fu ? '\n【追问】' + fu : '') + (srcUrl ? '\n【原文出处链接（务必原样放进卡片背面，做成可点链接）】' + srcUrl : '');
    var jobUi = startBgJob('制 Anki 中…');
    var ov = {}; try { if (spec.aiParams) ov = spec.aiParams() || {}; } catch (e) {}
    fetch('/pdf/api/snippets-to-async', {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ snippets: [{ text: text, source: sentence }], make_note: false, make_anki: true, note_name: '', model: ov.model || '', effort: ov.effort || '' })
    }).then(function (r) { return r.json(); }).then(function (d) {
      if (!d.ok || !d.job_id) { failBgJob(jobUi, d.error || '提交失败'); return; }
      pollBgJob(d.job_id, jobUi);
    }).catch(function (e) { failBgJob(jobUi, e.message); });
  }
  // ── 底部「继续追问」:带原句+译文+已有分析作上下文,流式回答追加到卡片内 ──
  function _followup(block, spec) {
    var inp = block.querySelector('.gb-fu-input');
    var q = (inp && inp.value || '').trim(); if (!q) return;
    inp.value = '';
    var sentence = block.dataset.src || '';
    var trans = (block.querySelector('.gb-trans') ? block.querySelector('.gb-trans').textContent : '').replace(/^🌐\s*/, '').trim();
    var analysis = (block.querySelector('.gb-content') ? block.querySelector('.gb-content').textContent : '').slice(0, 3000);
    var prev = (block.querySelector('.gb-fu-answers') ? block.querySelector('.gb-fu-answers').textContent : '').slice(-1500);
    var context = '【句子】' + sentence + (trans ? '\n【译文】' + trans : '') + '\n【已有语法分析】\n' + analysis + (prev ? '\n【之前的追问】\n' + prev : '');
    var ans = block.querySelector('.gb-fu-answers');
    var qDiv = document.createElement('div'); qDiv.className = 'gb-fu-q'; qDiv.textContent = '问：' + q; ans.appendChild(qDiv);
    var aDiv = document.createElement('div'); aDiv.className = 'gb-fu-a'; aDiv.innerHTML = '<span class="gb-loading">⏳</span>'; ans.appendChild(aDiv);
    var ov = {}; try { if (spec.aiParams) ov = spec.aiParams() || {}; } catch (e) {}
    var render = function (t) { aDiv.innerHTML = md(t || ' '); typeset(aDiv); };
    aiStreamFn('/pdf/api/explain', {
      method: 'POST', onText: render,
      body: { text: q, context: '基于这句的语法分析继续回答：\n' + context, model: ov.model || '', effort: ov.effort || '' }
    }).then(function (res) {
      if (res.ok && res.text) render(res.text);
      else if (!res.ok) aDiv.innerHTML = '追问失败：' + esc(res.error || '失败');
      else aDiv.innerHTML = '(无回答)';
    }).catch(function (e) { aDiv.innerHTML = '追问失败：' + esc(e.message); });
  }

  // ════════════════════════════════════════════════════════════════════
  // 历史:按书本持久(state/grammar-history/<sha>.json),新旧倒序(最新在上)。
  // ════════════════════════════════════════════════════════════════════
  function loadHistory(container, file, opts) {
    opts = opts || {};
    container = resolveContainer(container);
    if (!container) return Promise.resolve([]);
    return fetch('/pdf/api/grammar-history?file=' + encodeURIComponent(file || '')).then(function (r) { return r.json(); })
      .then(function (d) {
        var items = (d && d.items) || [];
        items.forEach(function (item) {
          addHistoryBlock(container, item, { file: file, aiParams: opts.aiParams, sourceUrl: opts.sourceUrl, viewModeKey: opts.viewModeKey });
        });
        return items;
      }).catch(function () { return []; });
  }

  // ════════════════════════════════════════════════════════════════════
  // 入口:分析一句话(选词/选区触发的「📊 语法」共同汇入此处)。
  // ════════════════════════════════════════════════════════════════════
  function analyze(opts) {
    opts = opts || {};
    var file = opts.file || '';
    var text = String(opts.text || '').trim();
    var sentenceRaw = opts.sentence != null ? opts.sentence : text;
    var doToast = opts.onToast || toast;
    if (!text) { doToast('没有选中内容'); return; }
    var run = function () {
      if (!_enabledBooks.length) {
        doToast('请启用至少一个语法 KG');
        try { opts.onNeedSetup && opts.onNeedSetup('no-kg'); } catch (e) {}
        return;
      }
      if (!_hasTracked) {
        doToast('已启用的 KG 中没有节点被跟踪，去技能树详情面板点「👁 跟踪」');
        try { opts.onNeedSetup && opts.onNeedSetup('no-tracked'); } catch (e) {}
        return;
      }
      var sentence = String(sentenceRaw || '').trim();
      if (sentence.length < 6) { doToast('句子太短'); return; }
      try { opts.onOpenPanel && opts.onOpenPanel(); } catch (e) {}
      var block = addLoadingBlock(opts.container, { sentence: sentence, text: text, file: file, sourceUrl: opts.sourceUrl, aiParams: opts.aiParams, viewModeKey: opts.viewModeKey });
      if (!block) return;
      safeFetch('/pdf/api/grammar-analyze', {
        method: 'POST', headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ text: text, sentence: sentence, file: file, enabled_books: _enabledBooks })
      }, { retries: 2 }).then(function (r) {
        if (!r.ok) {
          return r.json().catch(function () { return {}; }).then(function (err) { fillBlockError(block, (err && err.error) || ('HTTP ' + r.status)); });
        }
        return r.json().then(function (d) {
          if (!d.ok) { fillBlockError(block, d.error || '?'); return; }
          fillBlock(block, d, sentence, opts.viewModeKey);
          if (d.engine === 'spacy') streamGrammar(block, { file: file, sentence: sentence, text: text, aiParams: opts.aiParams, viewModeKey: opts.viewModeKey });
        });
      }).catch(function (e) { fillBlockError(block, e.message); });
    };
    if (_loaded) run(); else loadTracked(file).then(run);
  }

  RC.grammar = {
    extractSentence: extractSentence,
    loadTracked: loadTracked,
    enabledBooks: function () { return _enabledBooks.slice(); },
    hasTracked: function () { return _hasTracked; },
    saveEnabledBooks: saveEnabledBooks,
    renderTrackList: renderTrackList,
    getViewMode: getViewMode,
    setViewMode: setViewMode,
    analyze: analyze,
    loadHistory: loadHistory,
    clearBlocks: clearBlocks,
    posColor: posColor,
    compColor: compColor
  };
})();

})(window.__bwReaderDoc || document, window.__bwReaderFetch || window.fetch.bind(window));
